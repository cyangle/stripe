#  Stripe API
#
# The Stripe REST API. Please see https://stripe.com/docs/api for more details.
#
# The version of the OpenAPI document: 2024-06-20
# Contact: dev-platform@stripe.com
# Generated by: https://openapi-generator.tech
#

require "../../core"

require "../models/deleted_invoiceitem"
require "../models/error"
require "../models/get_accounts_created_parameter"

require "../models/invoiceitem"
require "../models/invoices_items_list"
require "../models/one_time_price_data"
require "../models/period"
require "../models/post_accounts_request_metadata"
require "../models/post_invoiceitems_invoiceitem_request_discounts"
require "../models/post_invoiceitems_invoiceitem_request_tax_rates"
require "../models/post_invoiceitems_request_discounts"

module Stripe
  class InvoiceitemsApi
    property api_client : ApiClient

    delegate client_side_validation?, debugging?, to: @api_client.config

    def initialize(api_client = ApiClient.default)
      @api_client = api_client
    end

    # <p>Deletes an invoice item, removing it from an invoice. Deleting invoice items is only possible when they’re not attached to invoices, or if it’s attached to a draft invoice.</p>
    # @required @param invoiceitem [String?]
    # @return [Stripe::DeletedInvoiceitem]
    def delete_invoiceitems_invoiceitem(
      *,
      invoiceitem : String? = nil
    ) : Stripe::DeletedInvoiceitem
      data, _status_code, _headers = delete_invoiceitems_invoiceitem_with_http_info(invoiceitem: invoiceitem)
      data
    end

    # &lt;p&gt;Deletes an invoice item, removing it from an invoice. Deleting invoice items is only possible when they’re not attached to invoices, or if it’s attached to a draft invoice.&lt;/p&gt;
    # @required @param invoiceitem [String?]
    # @return [Tuple(Stripe::DeletedInvoiceitem, Integer, Hash)] Stripe::DeletedInvoiceitem, response status code and response headers
    def delete_invoiceitems_invoiceitem_with_http_info(
      *,
      invoiceitem : String? = nil
    ) : Tuple(Stripe::DeletedInvoiceitem, Int32, Hash(String, Array(String) | String))
      request = build_api_request_for_delete_invoiceitems_invoiceitem(invoiceitem: invoiceitem)

      body, status_code, headers = @api_client.execute_api_request(request)

      if debugging?
        Log.debug { "API called: InvoiceitemsApi#delete_invoiceitems_invoiceitem\nBody: #{body.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}" }
      end

      Tuple.new(Stripe::DeletedInvoiceitem.from_json(body), status_code, headers)
    end

    # &lt;p&gt;Deletes an invoice item, removing it from an invoice. Deleting invoice items is only possible when they’re not attached to invoices, or if it’s attached to a draft invoice.&lt;/p&gt;
    # @required @param invoiceitem [String?]
    # @return nil
    def delete_invoiceitems_invoiceitem(
      *,
      invoiceitem : String? = nil,
      &block : Crest::Response ->
    ) : Nil
      build_api_request_for_delete_invoiceitems_invoiceitem(invoiceitem: invoiceitem).execute(&block)
    end

    DELETE_INVOICEITEMS_INVOICEITEM_MAX_LENGTH_FOR_INVOICEITEM = 5000

    # @return Crest::Request
    def build_api_request_for_delete_invoiceitems_invoiceitem(
      *,
      invoiceitem : String? = nil
    ) : Crest::Request
      if debugging?
        Log.debug { "Calling API: InvoiceitemsApi.delete_invoiceitems_invoiceitem ..." }
      end

      if client_side_validation?
        raise ArgumentError.new("\"invoiceitem\" is required and cannot be null") if invoiceitem.nil?
        unless (_invoiceitem = invoiceitem).nil?
          OpenApi::PrimitiveValidator.validate_max_length("invoiceitem", invoiceitem.to_s.size, DELETE_INVOICEITEMS_INVOICEITEM_MAX_LENGTH_FOR_INVOICEITEM)
        end
      end

      # resource path
      local_var_path = "/v1/invoiceitems/{invoiceitem}".sub("{" + "invoiceitem" + "}", URI.encode_path(invoiceitem.to_s))

      # header parameters
      header_params : Hash(String, String) = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # cookie parameters
      cookie_params : Hash(String, String) = Hash(String, String).new

      # query parameters
      query_params : Hash(String, (String | Array(String) | JSON::Any)) = Hash(String, (String | Array(String) | JSON::Any)).new

      # form parameters
      form_params : Array(Tuple(String, Crest::ParamsValue)) | Nil = nil

      # http body (model)
      post_body : IO | String | Nil = nil

      # auth_names
      auth_names = ["basicAuth", "bearerAuth"]

      @api_client.build_api_request(
        http_method: :"DELETE",
        path: local_var_path,
        operation: "InvoiceitemsApi.delete_invoiceitems_invoiceitem",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        cookie_params: cookie_params,
        query_params: query_params,
        form_params: form_params
      )
    end

    # <p>Returns a list of your invoice items. Invoice items are returned sorted by creation date, with the most recently created invoice items appearing first.</p>
    # @optional @param ending_before [String?] A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    # @optional @param starting_after [String?] A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    # @optional @param limit [Int32?] A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    # @optional @param invoice [String?] Only return invoice items belonging to this invoice. If none is provided, all invoice items will be returned. If specifying an invoice, no customer identifier is needed.
    # @optional @param created [Stripe::GetAccountsCreatedParameter?] Only return invoice items that were created during the given date interval.
    # @optional @param pending [Bool?] Set to `true` to only show pending invoice items, which are not yet attached to any invoices. Set to `false` to only show invoice items already attached to invoices. If unspecified, no filter is applied.
    # @optional @param expand [Array(Array(String))?] Specifies which fields in the response should be expanded.
    # @optional @param customer [String?] The identifier of the customer whose invoice items to return. If none is provided, all invoice items will be returned.
    # @return [Stripe::InvoicesItemsList]
    def get_invoiceitems(
      *,
      ending_before : String? = nil,
      starting_after : String? = nil,
      limit : Int64? = nil,
      invoice : String? = nil,
      created : Stripe::GetAccountsCreatedParameter? = nil,
      pending : Bool? = nil,
      expand : Array(Array(String))? = nil,
      customer : String? = nil
    ) : Stripe::InvoicesItemsList
      data, _status_code, _headers = get_invoiceitems_with_http_info(ending_before: ending_before, starting_after: starting_after, limit: limit, invoice: invoice, created: created, pending: pending, expand: expand, customer: customer)
      data
    end

    # &lt;p&gt;Returns a list of your invoice items. Invoice items are returned sorted by creation date, with the most recently created invoice items appearing first.&lt;/p&gt;
    # @optional @param ending_before [String?] A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    # @optional @param starting_after [String?] A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    # @optional @param limit [Int32?] A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    # @optional @param invoice [String?] Only return invoice items belonging to this invoice. If none is provided, all invoice items will be returned. If specifying an invoice, no customer identifier is needed.
    # @optional @param created [Stripe::GetAccountsCreatedParameter?] Only return invoice items that were created during the given date interval.
    # @optional @param pending [Bool?] Set to `true` to only show pending invoice items, which are not yet attached to any invoices. Set to `false` to only show invoice items already attached to invoices. If unspecified, no filter is applied.
    # @optional @param expand [Array(Array(String))?] Specifies which fields in the response should be expanded.
    # @optional @param customer [String?] The identifier of the customer whose invoice items to return. If none is provided, all invoice items will be returned.
    # @return [Tuple(Stripe::InvoicesItemsList, Integer, Hash)] Stripe::InvoicesItemsList, response status code and response headers
    def get_invoiceitems_with_http_info(
      *,
      ending_before : String? = nil,
      starting_after : String? = nil,
      limit : Int64? = nil,
      invoice : String? = nil,
      created : Stripe::GetAccountsCreatedParameter? = nil,
      pending : Bool? = nil,
      expand : Array(Array(String))? = nil,
      customer : String? = nil
    ) : Tuple(Stripe::InvoicesItemsList, Int32, Hash(String, Array(String) | String))
      request = build_api_request_for_get_invoiceitems(ending_before: ending_before, starting_after: starting_after, limit: limit, invoice: invoice, created: created, pending: pending, expand: expand, customer: customer)

      body, status_code, headers = @api_client.execute_api_request(request)

      if debugging?
        Log.debug { "API called: InvoiceitemsApi#get_invoiceitems\nBody: #{body.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}" }
      end

      Tuple.new(Stripe::InvoicesItemsList.from_json(body), status_code, headers)
    end

    # &lt;p&gt;Returns a list of your invoice items. Invoice items are returned sorted by creation date, with the most recently created invoice items appearing first.&lt;/p&gt;
    # @optional @param ending_before [String?] A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    # @optional @param starting_after [String?] A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    # @optional @param limit [Int32?] A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    # @optional @param invoice [String?] Only return invoice items belonging to this invoice. If none is provided, all invoice items will be returned. If specifying an invoice, no customer identifier is needed.
    # @optional @param created [Stripe::GetAccountsCreatedParameter?] Only return invoice items that were created during the given date interval.
    # @optional @param pending [Bool?] Set to `true` to only show pending invoice items, which are not yet attached to any invoices. Set to `false` to only show invoice items already attached to invoices. If unspecified, no filter is applied.
    # @optional @param expand [Array(Array(String))?] Specifies which fields in the response should be expanded.
    # @optional @param customer [String?] The identifier of the customer whose invoice items to return. If none is provided, all invoice items will be returned.
    # @return nil
    def get_invoiceitems(
      *,
      ending_before : String? = nil,
      starting_after : String? = nil,
      limit : Int64? = nil,
      invoice : String? = nil,
      created : Stripe::GetAccountsCreatedParameter? = nil,
      pending : Bool? = nil,
      expand : Array(Array(String))? = nil,
      customer : String? = nil,
      &block : Crest::Response ->
    ) : Nil
      build_api_request_for_get_invoiceitems(ending_before: ending_before, starting_after: starting_after, limit: limit, invoice: invoice, created: created, pending: pending, expand: expand, customer: customer).execute(&block)
    end

    GET_INVOICEITEMS_MAX_LENGTH_FOR_ENDING_BEFORE  = 5000
    GET_INVOICEITEMS_MAX_LENGTH_FOR_STARTING_AFTER = 5000
    GET_INVOICEITEMS_MAX_LENGTH_FOR_INVOICE        = 5000
    GET_INVOICEITEMS_MAX_LENGTH_FOR_CUSTOMER       = 5000

    # @return Crest::Request
    def build_api_request_for_get_invoiceitems(
      *,
      ending_before : String? = nil,
      starting_after : String? = nil,
      limit : Int64? = nil,
      invoice : String? = nil,
      created : Stripe::GetAccountsCreatedParameter? = nil,
      pending : Bool? = nil,
      expand : Array(Array(String))? = nil,
      customer : String? = nil
    ) : Crest::Request
      if debugging?
        Log.debug { "Calling API: InvoiceitemsApi.get_invoiceitems ..." }
      end

      if client_side_validation?
        unless (_ending_before = ending_before).nil?
          OpenApi::PrimitiveValidator.validate_max_length("ending_before", ending_before.to_s.size, GET_INVOICEITEMS_MAX_LENGTH_FOR_ENDING_BEFORE)
        end
        unless (_starting_after = starting_after).nil?
          OpenApi::PrimitiveValidator.validate_max_length("starting_after", starting_after.to_s.size, GET_INVOICEITEMS_MAX_LENGTH_FOR_STARTING_AFTER)
        end

        unless (_invoice = invoice).nil?
          OpenApi::PrimitiveValidator.validate_max_length("invoice", invoice.to_s.size, GET_INVOICEITEMS_MAX_LENGTH_FOR_INVOICE)
        end
        unless (_created = created).nil?
          _created.validate if _created.is_a?(OpenApi::Validatable)
        end

        unless (_customer = customer).nil?
          OpenApi::PrimitiveValidator.validate_max_length("customer", customer.to_s.size, GET_INVOICEITEMS_MAX_LENGTH_FOR_CUSTOMER)
        end
      end

      # resource path
      local_var_path = "/v1/invoiceitems"

      # header parameters
      header_params : Hash(String, String) = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # cookie parameters
      cookie_params : Hash(String, String) = Hash(String, String).new

      # query parameters
      query_params : Hash(String, (String | Array(String) | JSON::Any)) = Hash(String, (String | Array(String) | JSON::Any)).new
      query_params["ending_before"] = ending_before.to_s if !ending_before.nil?
      query_params["starting_after"] = starting_after.to_s if !starting_after.nil?
      query_params["limit"] = limit.to_s if !limit.nil?
      query_params["invoice"] = invoice.to_s if !invoice.nil?
      query_params["created"] = created.to_s if !created.nil?
      query_params["pending"] = pending.to_s if !pending.nil?
      query_params["expand"] = @api_client.build_collection_param(expand, "csv") if !expand.nil? && !expand.empty?
      query_params["customer"] = customer.to_s if !customer.nil?

      # form parameters
      form_params : Array(Tuple(String, Crest::ParamsValue)) | Nil = nil

      # http body (model)
      post_body : IO | String | Nil = nil

      # auth_names
      auth_names = ["basicAuth", "bearerAuth"]

      @api_client.build_api_request(
        http_method: :"GET",
        path: local_var_path,
        operation: "InvoiceitemsApi.get_invoiceitems",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        cookie_params: cookie_params,
        query_params: query_params,
        form_params: form_params
      )
    end

    # <p>Retrieves the invoice item with the given ID.</p>
    # @required @param invoiceitem [String?]
    # @optional @param expand [Array(Array(String))?] Specifies which fields in the response should be expanded.
    # @return [Stripe::Invoiceitem]
    def get_invoiceitems_invoiceitem(
      *,
      invoiceitem : String? = nil,
      expand : Array(Array(String))? = nil
    ) : Stripe::Invoiceitem
      data, _status_code, _headers = get_invoiceitems_invoiceitem_with_http_info(invoiceitem: invoiceitem, expand: expand)
      data
    end

    # &lt;p&gt;Retrieves the invoice item with the given ID.&lt;/p&gt;
    # @required @param invoiceitem [String?]
    # @optional @param expand [Array(Array(String))?] Specifies which fields in the response should be expanded.
    # @return [Tuple(Stripe::Invoiceitem, Integer, Hash)] Stripe::Invoiceitem, response status code and response headers
    def get_invoiceitems_invoiceitem_with_http_info(
      *,
      invoiceitem : String? = nil,
      expand : Array(Array(String))? = nil
    ) : Tuple(Stripe::Invoiceitem, Int32, Hash(String, Array(String) | String))
      request = build_api_request_for_get_invoiceitems_invoiceitem(invoiceitem: invoiceitem, expand: expand)

      body, status_code, headers = @api_client.execute_api_request(request)

      if debugging?
        Log.debug { "API called: InvoiceitemsApi#get_invoiceitems_invoiceitem\nBody: #{body.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}" }
      end

      Tuple.new(Stripe::Invoiceitem.from_json(body), status_code, headers)
    end

    # &lt;p&gt;Retrieves the invoice item with the given ID.&lt;/p&gt;
    # @required @param invoiceitem [String?]
    # @optional @param expand [Array(Array(String))?] Specifies which fields in the response should be expanded.
    # @return nil
    def get_invoiceitems_invoiceitem(
      *,
      invoiceitem : String? = nil,
      expand : Array(Array(String))? = nil,
      &block : Crest::Response ->
    ) : Nil
      build_api_request_for_get_invoiceitems_invoiceitem(invoiceitem: invoiceitem, expand: expand).execute(&block)
    end

    GET_INVOICEITEMS_INVOICEITEM_MAX_LENGTH_FOR_INVOICEITEM = 5000

    # @return Crest::Request
    def build_api_request_for_get_invoiceitems_invoiceitem(
      *,
      invoiceitem : String? = nil,
      expand : Array(Array(String))? = nil
    ) : Crest::Request
      if debugging?
        Log.debug { "Calling API: InvoiceitemsApi.get_invoiceitems_invoiceitem ..." }
      end

      if client_side_validation?
        raise ArgumentError.new("\"invoiceitem\" is required and cannot be null") if invoiceitem.nil?
        unless (_invoiceitem = invoiceitem).nil?
          OpenApi::PrimitiveValidator.validate_max_length("invoiceitem", invoiceitem.to_s.size, GET_INVOICEITEMS_INVOICEITEM_MAX_LENGTH_FOR_INVOICEITEM)
        end
      end

      # resource path
      local_var_path = "/v1/invoiceitems/{invoiceitem}".sub("{" + "invoiceitem" + "}", URI.encode_path(invoiceitem.to_s))

      # header parameters
      header_params : Hash(String, String) = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # cookie parameters
      cookie_params : Hash(String, String) = Hash(String, String).new

      # query parameters
      query_params : Hash(String, (String | Array(String) | JSON::Any)) = Hash(String, (String | Array(String) | JSON::Any)).new
      query_params["expand"] = @api_client.build_collection_param(expand, "csv") if !expand.nil? && !expand.empty?

      # form parameters
      form_params : Array(Tuple(String, Crest::ParamsValue)) | Nil = nil

      # http body (model)
      post_body : IO | String | Nil = nil

      # auth_names
      auth_names = ["basicAuth", "bearerAuth"]

      @api_client.build_api_request(
        http_method: :"GET",
        path: local_var_path,
        operation: "InvoiceitemsApi.get_invoiceitems_invoiceitem",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        cookie_params: cookie_params,
        query_params: query_params,
        form_params: form_params
      )
    end

    # <p>Creates an item to be added to a draft invoice (up to 250 items per invoice). If no invoice is specified, the item will be on the next invoice created for the customer specified.</p>
    # @required @param customer [String?] The ID of the customer who will be billed when this invoice item is billed.
    # @optional @param amount [Int32?] The integer amount in cents (or local equivalent) of the charge to be applied to the upcoming invoice. Passing in a negative `amount` will reduce the `amount_due` on the invoice.
    # @optional @param currency [String?] Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
    # @optional @param description [String?] An arbitrary string which you can attach to the invoice item. The description is displayed in the invoice for easy tracking.
    # @optional @param discountable [Bool?] Controls whether discounts apply to this invoice item. Defaults to false for prorations or negative invoice items, and true for all other invoice items.
    # @optional @param discounts [Stripe::PostInvoiceitemsRequestDiscounts?]
    # @optional @param expand [Array(String)?] Specifies which fields in the response should be expanded.
    # @optional @param invoice [String?] The ID of an existing invoice to add this invoice item to. When left blank, the invoice item will be added to the next upcoming scheduled invoice. This is useful when adding invoice items in response to an invoice.created webhook. You can only add invoice items to draft invoices and there is a maximum of 250 items per invoice.
    # @optional @param metadata [Stripe::PostAccountsRequestMetadata?]
    # @optional @param period [Stripe::Period?]
    # @optional @param price [String?] The ID of the price object. One of `price` or `price_data` is required.
    # @optional @param price_data [Stripe::OneTimePriceData?]
    # @optional @param quantity [Int32?] Non-negative integer. The quantity of units for the invoice item.
    # @optional @param subscription [String?] The ID of a subscription to add this invoice item to. When left blank, the invoice item is added to the next upcoming scheduled invoice. When set, scheduled invoices for subscriptions other than the specified subscription will ignore the invoice item. Use this when you want to express that an invoice item has been accrued within the context of a particular subscription.
    # @optional @param tax_behavior [String?] Only required if a [default tax behavior](https://stripe.com/docs/tax/products-prices-tax-categories-tax-behavior#setting-a-default-tax-behavior-(recommended)) was not provided in the Stripe Tax settings. Specifies whether the price is considered inclusive of taxes or exclusive of taxes. One of `inclusive`, `exclusive`, or `unspecified`. Once specified as either `inclusive` or `exclusive`, it cannot be changed.
    # @optional @param tax_code [String?]
    # @optional @param tax_rates [Array(String)?] The tax rates which apply to the invoice item. When set, the `default_tax_rates` on the invoice do not apply to this invoice item.
    # @optional @param unit_amount [Int32?] The integer unit amount in cents (or local equivalent) of the charge to be applied to the upcoming invoice. This `unit_amount` will be multiplied by the quantity to get the full amount. Passing in a negative `unit_amount` will reduce the `amount_due` on the invoice.
    # @optional @param unit_amount_decimal [BigDecimal?] Same as `unit_amount`, but accepts a decimal value in cents (or local equivalent) with at most 12 decimal places. Only one of `unit_amount` and `unit_amount_decimal` can be set.
    # @return [Stripe::Invoiceitem]
    def post_invoiceitems(
      *,
      customer : String? = nil,
      amount : Int64? = nil,
      currency : String? = nil,
      description : String? = nil,
      discountable : Bool? = nil,
      discounts : Stripe::PostInvoiceitemsRequestDiscounts? = nil,
      expand : Array(String)? = nil,
      invoice : String? = nil,
      metadata : Stripe::PostAccountsRequestMetadata? = nil,
      period : Stripe::Period? = nil,
      price : String? = nil,
      price_data : Stripe::OneTimePriceData? = nil,
      quantity : Int64? = nil,
      subscription : String? = nil,
      tax_behavior : String? = nil,
      tax_code : String? = nil,
      tax_rates : Array(String)? = nil,
      unit_amount : Int64? = nil,
      unit_amount_decimal : BigDecimal? = nil
    ) : Stripe::Invoiceitem
      data, _status_code, _headers = post_invoiceitems_with_http_info(customer: customer, amount: amount, currency: currency, description: description, discountable: discountable, discounts: discounts, expand: expand, invoice: invoice, metadata: metadata, period: period, price: price, price_data: price_data, quantity: quantity, subscription: subscription, tax_behavior: tax_behavior, tax_code: tax_code, tax_rates: tax_rates, unit_amount: unit_amount, unit_amount_decimal: unit_amount_decimal)
      data
    end

    # &lt;p&gt;Creates an item to be added to a draft invoice (up to 250 items per invoice). If no invoice is specified, the item will be on the next invoice created for the customer specified.&lt;/p&gt;
    # @required @param customer [String?] The ID of the customer who will be billed when this invoice item is billed.
    # @optional @param amount [Int32?] The integer amount in cents (or local equivalent) of the charge to be applied to the upcoming invoice. Passing in a negative `amount` will reduce the `amount_due` on the invoice.
    # @optional @param currency [String?] Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
    # @optional @param description [String?] An arbitrary string which you can attach to the invoice item. The description is displayed in the invoice for easy tracking.
    # @optional @param discountable [Bool?] Controls whether discounts apply to this invoice item. Defaults to false for prorations or negative invoice items, and true for all other invoice items.
    # @optional @param discounts [Stripe::PostInvoiceitemsRequestDiscounts?]
    # @optional @param expand [Array(String)?] Specifies which fields in the response should be expanded.
    # @optional @param invoice [String?] The ID of an existing invoice to add this invoice item to. When left blank, the invoice item will be added to the next upcoming scheduled invoice. This is useful when adding invoice items in response to an invoice.created webhook. You can only add invoice items to draft invoices and there is a maximum of 250 items per invoice.
    # @optional @param metadata [Stripe::PostAccountsRequestMetadata?]
    # @optional @param period [Stripe::Period?]
    # @optional @param price [String?] The ID of the price object. One of `price` or `price_data` is required.
    # @optional @param price_data [Stripe::OneTimePriceData?]
    # @optional @param quantity [Int32?] Non-negative integer. The quantity of units for the invoice item.
    # @optional @param subscription [String?] The ID of a subscription to add this invoice item to. When left blank, the invoice item is added to the next upcoming scheduled invoice. When set, scheduled invoices for subscriptions other than the specified subscription will ignore the invoice item. Use this when you want to express that an invoice item has been accrued within the context of a particular subscription.
    # @optional @param tax_behavior [String?] Only required if a [default tax behavior](https://stripe.com/docs/tax/products-prices-tax-categories-tax-behavior#setting-a-default-tax-behavior-(recommended)) was not provided in the Stripe Tax settings. Specifies whether the price is considered inclusive of taxes or exclusive of taxes. One of `inclusive`, `exclusive`, or `unspecified`. Once specified as either `inclusive` or `exclusive`, it cannot be changed.
    # @optional @param tax_code [String?]
    # @optional @param tax_rates [Array(String)?] The tax rates which apply to the invoice item. When set, the `default_tax_rates` on the invoice do not apply to this invoice item.
    # @optional @param unit_amount [Int32?] The integer unit amount in cents (or local equivalent) of the charge to be applied to the upcoming invoice. This `unit_amount` will be multiplied by the quantity to get the full amount. Passing in a negative `unit_amount` will reduce the `amount_due` on the invoice.
    # @optional @param unit_amount_decimal [BigDecimal?] Same as `unit_amount`, but accepts a decimal value in cents (or local equivalent) with at most 12 decimal places. Only one of `unit_amount` and `unit_amount_decimal` can be set.
    # @return [Tuple(Stripe::Invoiceitem, Integer, Hash)] Stripe::Invoiceitem, response status code and response headers
    def post_invoiceitems_with_http_info(
      *,
      customer : String? = nil,
      amount : Int64? = nil,
      currency : String? = nil,
      description : String? = nil,
      discountable : Bool? = nil,
      discounts : Stripe::PostInvoiceitemsRequestDiscounts? = nil,
      expand : Array(String)? = nil,
      invoice : String? = nil,
      metadata : Stripe::PostAccountsRequestMetadata? = nil,
      period : Stripe::Period? = nil,
      price : String? = nil,
      price_data : Stripe::OneTimePriceData? = nil,
      quantity : Int64? = nil,
      subscription : String? = nil,
      tax_behavior : String? = nil,
      tax_code : String? = nil,
      tax_rates : Array(String)? = nil,
      unit_amount : Int64? = nil,
      unit_amount_decimal : BigDecimal? = nil
    ) : Tuple(Stripe::Invoiceitem, Int32, Hash(String, Array(String) | String))
      request = build_api_request_for_post_invoiceitems(customer: customer, amount: amount, currency: currency, description: description, discountable: discountable, discounts: discounts, expand: expand, invoice: invoice, metadata: metadata, period: period, price: price, price_data: price_data, quantity: quantity, subscription: subscription, tax_behavior: tax_behavior, tax_code: tax_code, tax_rates: tax_rates, unit_amount: unit_amount, unit_amount_decimal: unit_amount_decimal)

      body, status_code, headers = @api_client.execute_api_request(request)

      if debugging?
        Log.debug { "API called: InvoiceitemsApi#post_invoiceitems\nBody: #{body.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}" }
      end

      Tuple.new(Stripe::Invoiceitem.from_json(body), status_code, headers)
    end

    # &lt;p&gt;Creates an item to be added to a draft invoice (up to 250 items per invoice). If no invoice is specified, the item will be on the next invoice created for the customer specified.&lt;/p&gt;
    # @required @param customer [String?] The ID of the customer who will be billed when this invoice item is billed.
    # @optional @param amount [Int32?] The integer amount in cents (or local equivalent) of the charge to be applied to the upcoming invoice. Passing in a negative `amount` will reduce the `amount_due` on the invoice.
    # @optional @param currency [String?] Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
    # @optional @param description [String?] An arbitrary string which you can attach to the invoice item. The description is displayed in the invoice for easy tracking.
    # @optional @param discountable [Bool?] Controls whether discounts apply to this invoice item. Defaults to false for prorations or negative invoice items, and true for all other invoice items.
    # @optional @param discounts [Stripe::PostInvoiceitemsRequestDiscounts?]
    # @optional @param expand [Array(String)?] Specifies which fields in the response should be expanded.
    # @optional @param invoice [String?] The ID of an existing invoice to add this invoice item to. When left blank, the invoice item will be added to the next upcoming scheduled invoice. This is useful when adding invoice items in response to an invoice.created webhook. You can only add invoice items to draft invoices and there is a maximum of 250 items per invoice.
    # @optional @param metadata [Stripe::PostAccountsRequestMetadata?]
    # @optional @param period [Stripe::Period?]
    # @optional @param price [String?] The ID of the price object. One of `price` or `price_data` is required.
    # @optional @param price_data [Stripe::OneTimePriceData?]
    # @optional @param quantity [Int32?] Non-negative integer. The quantity of units for the invoice item.
    # @optional @param subscription [String?] The ID of a subscription to add this invoice item to. When left blank, the invoice item is added to the next upcoming scheduled invoice. When set, scheduled invoices for subscriptions other than the specified subscription will ignore the invoice item. Use this when you want to express that an invoice item has been accrued within the context of a particular subscription.
    # @optional @param tax_behavior [String?] Only required if a [default tax behavior](https://stripe.com/docs/tax/products-prices-tax-categories-tax-behavior#setting-a-default-tax-behavior-(recommended)) was not provided in the Stripe Tax settings. Specifies whether the price is considered inclusive of taxes or exclusive of taxes. One of `inclusive`, `exclusive`, or `unspecified`. Once specified as either `inclusive` or `exclusive`, it cannot be changed.
    # @optional @param tax_code [String?]
    # @optional @param tax_rates [Array(String)?] The tax rates which apply to the invoice item. When set, the `default_tax_rates` on the invoice do not apply to this invoice item.
    # @optional @param unit_amount [Int32?] The integer unit amount in cents (or local equivalent) of the charge to be applied to the upcoming invoice. This `unit_amount` will be multiplied by the quantity to get the full amount. Passing in a negative `unit_amount` will reduce the `amount_due` on the invoice.
    # @optional @param unit_amount_decimal [BigDecimal?] Same as `unit_amount`, but accepts a decimal value in cents (or local equivalent) with at most 12 decimal places. Only one of `unit_amount` and `unit_amount_decimal` can be set.
    # @return nil
    def post_invoiceitems(
      *,
      customer : String? = nil,
      amount : Int64? = nil,
      currency : String? = nil,
      description : String? = nil,
      discountable : Bool? = nil,
      discounts : Stripe::PostInvoiceitemsRequestDiscounts? = nil,
      expand : Array(String)? = nil,
      invoice : String? = nil,
      metadata : Stripe::PostAccountsRequestMetadata? = nil,
      period : Stripe::Period? = nil,
      price : String? = nil,
      price_data : Stripe::OneTimePriceData? = nil,
      quantity : Int64? = nil,
      subscription : String? = nil,
      tax_behavior : String? = nil,
      tax_code : String? = nil,
      tax_rates : Array(String)? = nil,
      unit_amount : Int64? = nil,
      unit_amount_decimal : BigDecimal? = nil,
      &block : Crest::Response ->
    ) : Nil
      build_api_request_for_post_invoiceitems(customer: customer, amount: amount, currency: currency, description: description, discountable: discountable, discounts: discounts, expand: expand, invoice: invoice, metadata: metadata, period: period, price: price, price_data: price_data, quantity: quantity, subscription: subscription, tax_behavior: tax_behavior, tax_code: tax_code, tax_rates: tax_rates, unit_amount: unit_amount, unit_amount_decimal: unit_amount_decimal).execute(&block)
    end

    POST_INVOICEITEMS_MAX_LENGTH_FOR_CUSTOMER       = 5000
    POST_INVOICEITEMS_MAX_LENGTH_FOR_DESCRIPTION    = 5000
    POST_INVOICEITEMS_MAX_LENGTH_FOR_INVOICE        = 5000
    POST_INVOICEITEMS_MAX_LENGTH_FOR_PRICE          = 5000
    POST_INVOICEITEMS_MAX_LENGTH_FOR_SUBSCRIPTION   = 5000
    POST_INVOICEITEMS_VALID_VALUES_FOR_TAX_BEHAVIOR = String.static_array("exclusive", "inclusive", "unspecified")
    POST_INVOICEITEMS_VALID_VALUES_FOR_TAX_CODE     = String.static_array("")

    # @return Crest::Request
    def build_api_request_for_post_invoiceitems(
      *,
      customer : String? = nil,
      amount : Int64? = nil,
      currency : String? = nil,
      description : String? = nil,
      discountable : Bool? = nil,
      discounts : Stripe::PostInvoiceitemsRequestDiscounts? = nil,
      expand : Array(String)? = nil,
      invoice : String? = nil,
      metadata : Stripe::PostAccountsRequestMetadata? = nil,
      period : Stripe::Period? = nil,
      price : String? = nil,
      price_data : Stripe::OneTimePriceData? = nil,
      quantity : Int64? = nil,
      subscription : String? = nil,
      tax_behavior : String? = nil,
      tax_code : String? = nil,
      tax_rates : Array(String)? = nil,
      unit_amount : Int64? = nil,
      unit_amount_decimal : BigDecimal? = nil
    ) : Crest::Request
      if debugging?
        Log.debug { "Calling API: InvoiceitemsApi.post_invoiceitems ..." }
      end

      if client_side_validation?
        raise ArgumentError.new("\"customer\" is required and cannot be null") if customer.nil?
        unless (_customer = customer).nil?
          OpenApi::PrimitiveValidator.validate_max_length("customer", customer.to_s.size, POST_INVOICEITEMS_MAX_LENGTH_FOR_CUSTOMER)
        end

        unless (_description = description).nil?
          OpenApi::PrimitiveValidator.validate_max_length("description", description.to_s.size, POST_INVOICEITEMS_MAX_LENGTH_FOR_DESCRIPTION)
        end

        unless (_discounts = discounts).nil?
          _discounts.validate if _discounts.is_a?(OpenApi::Validatable)
        end

        unless (_invoice = invoice).nil?
          OpenApi::PrimitiveValidator.validate_max_length("invoice", invoice.to_s.size, POST_INVOICEITEMS_MAX_LENGTH_FOR_INVOICE)
        end
        unless (_metadata = metadata).nil?
          _metadata.validate if _metadata.is_a?(OpenApi::Validatable)
        end
        unless (_period = period).nil?
          _period.validate if _period.is_a?(OpenApi::Validatable)
        end
        unless (_price = price).nil?
          OpenApi::PrimitiveValidator.validate_max_length("price", price.to_s.size, POST_INVOICEITEMS_MAX_LENGTH_FOR_PRICE)
        end
        unless (_price_data = price_data).nil?
          _price_data.validate if _price_data.is_a?(OpenApi::Validatable)
        end

        unless (_subscription = subscription).nil?
          OpenApi::PrimitiveValidator.validate_max_length("subscription", subscription.to_s.size, POST_INVOICEITEMS_MAX_LENGTH_FOR_SUBSCRIPTION)
        end
        unless (_tax_behavior = tax_behavior).nil?
          OpenApi::EnumValidator.validate("tax_behavior", _tax_behavior, POST_INVOICEITEMS_VALID_VALUES_FOR_TAX_BEHAVIOR)
        end
        unless (_tax_code = tax_code).nil?
          OpenApi::EnumValidator.validate("tax_code", _tax_code, POST_INVOICEITEMS_VALID_VALUES_FOR_TAX_CODE)
        end
      end

      # resource path
      local_var_path = "/v1/invoiceitems"

      # header parameters
      header_params : Hash(String, String) = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])
      # HTTP header "Content-Type"
      header_params["Content-Type"] = @api_client.select_header_content_type(["application/x-www-form-urlencoded"])

      # cookie parameters
      cookie_params : Hash(String, String) = Hash(String, String).new

      # query parameters
      query_params : Hash(String, (String | Array(String) | JSON::Any)) = Hash(String, (String | Array(String) | JSON::Any)).new

      # form parameters
      form_params : Array(Tuple(String, Crest::ParamsValue)) | Nil = Array(Tuple(String, Crest::ParamsValue)).new
      form_params << Tuple(String, Crest::ParamsValue).new("amount", amount.to_s) if !amount.nil?
      form_params << Tuple(String, Crest::ParamsValue).new("currency", currency.to_s) if !currency.nil?
      form_params << Tuple(String, Crest::ParamsValue).new("customer", customer.to_s) if !customer.nil?
      form_params << Tuple(String, Crest::ParamsValue).new("description", description.to_s) if !description.nil?
      form_params << Tuple(String, Crest::ParamsValue).new("discountable", discountable.to_s) if !discountable.nil?
      form_params.concat(Crest::ZeroEnumeratedFlatParamsEncoder.flatten_params(JSON.parse(discounts.to_json), "discounts")) if !discounts.nil?
      form_params.concat(Crest::ZeroEnumeratedFlatParamsEncoder.flatten_params(JSON.parse(expand.to_json), "expand")) if !expand.nil?
      form_params << Tuple(String, Crest::ParamsValue).new("invoice", invoice.to_s) if !invoice.nil?
      form_params.concat(Crest::ZeroEnumeratedFlatParamsEncoder.flatten_params(JSON.parse(metadata.to_json), "metadata")) if !metadata.nil?
      form_params.concat(Crest::ZeroEnumeratedFlatParamsEncoder.flatten_params(JSON.parse(period.to_json), "period")) if !period.nil?
      form_params << Tuple(String, Crest::ParamsValue).new("price", price.to_s) if !price.nil?
      form_params.concat(Crest::ZeroEnumeratedFlatParamsEncoder.flatten_params(JSON.parse(price_data.to_json), "price_data")) if !price_data.nil?
      form_params << Tuple(String, Crest::ParamsValue).new("quantity", quantity.to_s) if !quantity.nil?
      form_params << Tuple(String, Crest::ParamsValue).new("subscription", subscription.to_s) if !subscription.nil?
      form_params << Tuple(String, Crest::ParamsValue).new("tax_behavior", tax_behavior.to_s) if !tax_behavior.nil?
      form_params << Tuple(String, Crest::ParamsValue).new("tax_code", tax_code.to_s) if !tax_code.nil?
      form_params.concat(Crest::ZeroEnumeratedFlatParamsEncoder.flatten_params(JSON.parse(tax_rates.to_json), "tax_rates")) if !tax_rates.nil?
      form_params << Tuple(String, Crest::ParamsValue).new("unit_amount", unit_amount.to_s) if !unit_amount.nil?
      form_params << Tuple(String, Crest::ParamsValue).new("unit_amount_decimal", unit_amount_decimal.to_s) if !unit_amount_decimal.nil?

      # http body (model)
      post_body : IO | String | Nil = nil

      # auth_names
      auth_names = ["basicAuth", "bearerAuth"]

      @api_client.build_api_request(
        http_method: :"POST",
        path: local_var_path,
        operation: "InvoiceitemsApi.post_invoiceitems",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        cookie_params: cookie_params,
        query_params: query_params,
        form_params: form_params
      )
    end

    # <p>Updates the amount or description of an invoice item on an upcoming invoice. Updating an invoice item is only possible before the invoice it’s attached to is closed.</p>
    # @required @param invoiceitem [String?]
    # @optional @param amount [Int32?] The integer amount in cents (or local equivalent) of the charge to be applied to the upcoming invoice. If you want to apply a credit to the customer's account, pass a negative amount.
    # @optional @param description [String?] An arbitrary string which you can attach to the invoice item. The description is displayed in the invoice for easy tracking.
    # @optional @param discountable [Bool?] Controls whether discounts apply to this invoice item. Defaults to false for prorations or negative invoice items, and true for all other invoice items. Cannot be set to true for prorations.
    # @optional @param discounts [Stripe::PostInvoiceitemsInvoiceitemRequestDiscounts?]
    # @optional @param expand [Array(String)?] Specifies which fields in the response should be expanded.
    # @optional @param metadata [Stripe::PostAccountsRequestMetadata?]
    # @optional @param period [Stripe::Period?]
    # @optional @param price [String?] The ID of the price object. One of `price` or `price_data` is required.
    # @optional @param price_data [Stripe::OneTimePriceData?]
    # @optional @param quantity [Int32?] Non-negative integer. The quantity of units for the invoice item.
    # @optional @param tax_behavior [String?] Only required if a [default tax behavior](https://stripe.com/docs/tax/products-prices-tax-categories-tax-behavior#setting-a-default-tax-behavior-(recommended)) was not provided in the Stripe Tax settings. Specifies whether the price is considered inclusive of taxes or exclusive of taxes. One of `inclusive`, `exclusive`, or `unspecified`. Once specified as either `inclusive` or `exclusive`, it cannot be changed.
    # @optional @param tax_code [String?]
    # @optional @param tax_rates [Stripe::PostInvoiceitemsInvoiceitemRequestTaxRates?]
    # @optional @param unit_amount [Int32?] The integer unit amount in cents (or local equivalent) of the charge to be applied to the upcoming invoice. This unit_amount will be multiplied by the quantity to get the full amount. If you want to apply a credit to the customer's account, pass a negative unit_amount.
    # @optional @param unit_amount_decimal [BigDecimal?] Same as `unit_amount`, but accepts a decimal value in cents (or local equivalent) with at most 12 decimal places. Only one of `unit_amount` and `unit_amount_decimal` can be set.
    # @return [Stripe::Invoiceitem]
    def post_invoiceitems_invoiceitem(
      *,
      invoiceitem : String? = nil,
      amount : Int64? = nil,
      description : String? = nil,
      discountable : Bool? = nil,
      discounts : Stripe::PostInvoiceitemsInvoiceitemRequestDiscounts? = nil,
      expand : Array(String)? = nil,
      metadata : Stripe::PostAccountsRequestMetadata? = nil,
      period : Stripe::Period? = nil,
      price : String? = nil,
      price_data : Stripe::OneTimePriceData? = nil,
      quantity : Int64? = nil,
      tax_behavior : String? = nil,
      tax_code : String? = nil,
      tax_rates : Stripe::PostInvoiceitemsInvoiceitemRequestTaxRates? = nil,
      unit_amount : Int64? = nil,
      unit_amount_decimal : BigDecimal? = nil
    ) : Stripe::Invoiceitem
      data, _status_code, _headers = post_invoiceitems_invoiceitem_with_http_info(invoiceitem: invoiceitem, amount: amount, description: description, discountable: discountable, discounts: discounts, expand: expand, metadata: metadata, period: period, price: price, price_data: price_data, quantity: quantity, tax_behavior: tax_behavior, tax_code: tax_code, tax_rates: tax_rates, unit_amount: unit_amount, unit_amount_decimal: unit_amount_decimal)
      data
    end

    # &lt;p&gt;Updates the amount or description of an invoice item on an upcoming invoice. Updating an invoice item is only possible before the invoice it’s attached to is closed.&lt;/p&gt;
    # @required @param invoiceitem [String?]
    # @optional @param amount [Int32?] The integer amount in cents (or local equivalent) of the charge to be applied to the upcoming invoice. If you want to apply a credit to the customer's account, pass a negative amount.
    # @optional @param description [String?] An arbitrary string which you can attach to the invoice item. The description is displayed in the invoice for easy tracking.
    # @optional @param discountable [Bool?] Controls whether discounts apply to this invoice item. Defaults to false for prorations or negative invoice items, and true for all other invoice items. Cannot be set to true for prorations.
    # @optional @param discounts [Stripe::PostInvoiceitemsInvoiceitemRequestDiscounts?]
    # @optional @param expand [Array(String)?] Specifies which fields in the response should be expanded.
    # @optional @param metadata [Stripe::PostAccountsRequestMetadata?]
    # @optional @param period [Stripe::Period?]
    # @optional @param price [String?] The ID of the price object. One of `price` or `price_data` is required.
    # @optional @param price_data [Stripe::OneTimePriceData?]
    # @optional @param quantity [Int32?] Non-negative integer. The quantity of units for the invoice item.
    # @optional @param tax_behavior [String?] Only required if a [default tax behavior](https://stripe.com/docs/tax/products-prices-tax-categories-tax-behavior#setting-a-default-tax-behavior-(recommended)) was not provided in the Stripe Tax settings. Specifies whether the price is considered inclusive of taxes or exclusive of taxes. One of `inclusive`, `exclusive`, or `unspecified`. Once specified as either `inclusive` or `exclusive`, it cannot be changed.
    # @optional @param tax_code [String?]
    # @optional @param tax_rates [Stripe::PostInvoiceitemsInvoiceitemRequestTaxRates?]
    # @optional @param unit_amount [Int32?] The integer unit amount in cents (or local equivalent) of the charge to be applied to the upcoming invoice. This unit_amount will be multiplied by the quantity to get the full amount. If you want to apply a credit to the customer's account, pass a negative unit_amount.
    # @optional @param unit_amount_decimal [BigDecimal?] Same as `unit_amount`, but accepts a decimal value in cents (or local equivalent) with at most 12 decimal places. Only one of `unit_amount` and `unit_amount_decimal` can be set.
    # @return [Tuple(Stripe::Invoiceitem, Integer, Hash)] Stripe::Invoiceitem, response status code and response headers
    def post_invoiceitems_invoiceitem_with_http_info(
      *,
      invoiceitem : String? = nil,
      amount : Int64? = nil,
      description : String? = nil,
      discountable : Bool? = nil,
      discounts : Stripe::PostInvoiceitemsInvoiceitemRequestDiscounts? = nil,
      expand : Array(String)? = nil,
      metadata : Stripe::PostAccountsRequestMetadata? = nil,
      period : Stripe::Period? = nil,
      price : String? = nil,
      price_data : Stripe::OneTimePriceData? = nil,
      quantity : Int64? = nil,
      tax_behavior : String? = nil,
      tax_code : String? = nil,
      tax_rates : Stripe::PostInvoiceitemsInvoiceitemRequestTaxRates? = nil,
      unit_amount : Int64? = nil,
      unit_amount_decimal : BigDecimal? = nil
    ) : Tuple(Stripe::Invoiceitem, Int32, Hash(String, Array(String) | String))
      request = build_api_request_for_post_invoiceitems_invoiceitem(invoiceitem: invoiceitem, amount: amount, description: description, discountable: discountable, discounts: discounts, expand: expand, metadata: metadata, period: period, price: price, price_data: price_data, quantity: quantity, tax_behavior: tax_behavior, tax_code: tax_code, tax_rates: tax_rates, unit_amount: unit_amount, unit_amount_decimal: unit_amount_decimal)

      body, status_code, headers = @api_client.execute_api_request(request)

      if debugging?
        Log.debug { "API called: InvoiceitemsApi#post_invoiceitems_invoiceitem\nBody: #{body.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}" }
      end

      Tuple.new(Stripe::Invoiceitem.from_json(body), status_code, headers)
    end

    # &lt;p&gt;Updates the amount or description of an invoice item on an upcoming invoice. Updating an invoice item is only possible before the invoice it’s attached to is closed.&lt;/p&gt;
    # @required @param invoiceitem [String?]
    # @optional @param amount [Int32?] The integer amount in cents (or local equivalent) of the charge to be applied to the upcoming invoice. If you want to apply a credit to the customer's account, pass a negative amount.
    # @optional @param description [String?] An arbitrary string which you can attach to the invoice item. The description is displayed in the invoice for easy tracking.
    # @optional @param discountable [Bool?] Controls whether discounts apply to this invoice item. Defaults to false for prorations or negative invoice items, and true for all other invoice items. Cannot be set to true for prorations.
    # @optional @param discounts [Stripe::PostInvoiceitemsInvoiceitemRequestDiscounts?]
    # @optional @param expand [Array(String)?] Specifies which fields in the response should be expanded.
    # @optional @param metadata [Stripe::PostAccountsRequestMetadata?]
    # @optional @param period [Stripe::Period?]
    # @optional @param price [String?] The ID of the price object. One of `price` or `price_data` is required.
    # @optional @param price_data [Stripe::OneTimePriceData?]
    # @optional @param quantity [Int32?] Non-negative integer. The quantity of units for the invoice item.
    # @optional @param tax_behavior [String?] Only required if a [default tax behavior](https://stripe.com/docs/tax/products-prices-tax-categories-tax-behavior#setting-a-default-tax-behavior-(recommended)) was not provided in the Stripe Tax settings. Specifies whether the price is considered inclusive of taxes or exclusive of taxes. One of `inclusive`, `exclusive`, or `unspecified`. Once specified as either `inclusive` or `exclusive`, it cannot be changed.
    # @optional @param tax_code [String?]
    # @optional @param tax_rates [Stripe::PostInvoiceitemsInvoiceitemRequestTaxRates?]
    # @optional @param unit_amount [Int32?] The integer unit amount in cents (or local equivalent) of the charge to be applied to the upcoming invoice. This unit_amount will be multiplied by the quantity to get the full amount. If you want to apply a credit to the customer's account, pass a negative unit_amount.
    # @optional @param unit_amount_decimal [BigDecimal?] Same as `unit_amount`, but accepts a decimal value in cents (or local equivalent) with at most 12 decimal places. Only one of `unit_amount` and `unit_amount_decimal` can be set.
    # @return nil
    def post_invoiceitems_invoiceitem(
      *,
      invoiceitem : String? = nil,
      amount : Int64? = nil,
      description : String? = nil,
      discountable : Bool? = nil,
      discounts : Stripe::PostInvoiceitemsInvoiceitemRequestDiscounts? = nil,
      expand : Array(String)? = nil,
      metadata : Stripe::PostAccountsRequestMetadata? = nil,
      period : Stripe::Period? = nil,
      price : String? = nil,
      price_data : Stripe::OneTimePriceData? = nil,
      quantity : Int64? = nil,
      tax_behavior : String? = nil,
      tax_code : String? = nil,
      tax_rates : Stripe::PostInvoiceitemsInvoiceitemRequestTaxRates? = nil,
      unit_amount : Int64? = nil,
      unit_amount_decimal : BigDecimal? = nil,
      &block : Crest::Response ->
    ) : Nil
      build_api_request_for_post_invoiceitems_invoiceitem(invoiceitem: invoiceitem, amount: amount, description: description, discountable: discountable, discounts: discounts, expand: expand, metadata: metadata, period: period, price: price, price_data: price_data, quantity: quantity, tax_behavior: tax_behavior, tax_code: tax_code, tax_rates: tax_rates, unit_amount: unit_amount, unit_amount_decimal: unit_amount_decimal).execute(&block)
    end

    POST_INVOICEITEMS_INVOICEITEM_MAX_LENGTH_FOR_INVOICEITEM    = 5000
    POST_INVOICEITEMS_INVOICEITEM_MAX_LENGTH_FOR_DESCRIPTION    = 5000
    POST_INVOICEITEMS_INVOICEITEM_MAX_LENGTH_FOR_PRICE          = 5000
    POST_INVOICEITEMS_INVOICEITEM_VALID_VALUES_FOR_TAX_BEHAVIOR = String.static_array("exclusive", "inclusive", "unspecified")
    POST_INVOICEITEMS_INVOICEITEM_VALID_VALUES_FOR_TAX_CODE     = String.static_array("")

    # @return Crest::Request
    def build_api_request_for_post_invoiceitems_invoiceitem(
      *,
      invoiceitem : String? = nil,
      amount : Int64? = nil,
      description : String? = nil,
      discountable : Bool? = nil,
      discounts : Stripe::PostInvoiceitemsInvoiceitemRequestDiscounts? = nil,
      expand : Array(String)? = nil,
      metadata : Stripe::PostAccountsRequestMetadata? = nil,
      period : Stripe::Period? = nil,
      price : String? = nil,
      price_data : Stripe::OneTimePriceData? = nil,
      quantity : Int64? = nil,
      tax_behavior : String? = nil,
      tax_code : String? = nil,
      tax_rates : Stripe::PostInvoiceitemsInvoiceitemRequestTaxRates? = nil,
      unit_amount : Int64? = nil,
      unit_amount_decimal : BigDecimal? = nil
    ) : Crest::Request
      if debugging?
        Log.debug { "Calling API: InvoiceitemsApi.post_invoiceitems_invoiceitem ..." }
      end

      if client_side_validation?
        raise ArgumentError.new("\"invoiceitem\" is required and cannot be null") if invoiceitem.nil?
        unless (_invoiceitem = invoiceitem).nil?
          OpenApi::PrimitiveValidator.validate_max_length("invoiceitem", invoiceitem.to_s.size, POST_INVOICEITEMS_INVOICEITEM_MAX_LENGTH_FOR_INVOICEITEM)
        end

        unless (_description = description).nil?
          OpenApi::PrimitiveValidator.validate_max_length("description", description.to_s.size, POST_INVOICEITEMS_INVOICEITEM_MAX_LENGTH_FOR_DESCRIPTION)
        end

        unless (_discounts = discounts).nil?
          _discounts.validate if _discounts.is_a?(OpenApi::Validatable)
        end

        unless (_metadata = metadata).nil?
          _metadata.validate if _metadata.is_a?(OpenApi::Validatable)
        end
        unless (_period = period).nil?
          _period.validate if _period.is_a?(OpenApi::Validatable)
        end
        unless (_price = price).nil?
          OpenApi::PrimitiveValidator.validate_max_length("price", price.to_s.size, POST_INVOICEITEMS_INVOICEITEM_MAX_LENGTH_FOR_PRICE)
        end
        unless (_price_data = price_data).nil?
          _price_data.validate if _price_data.is_a?(OpenApi::Validatable)
        end

        unless (_tax_behavior = tax_behavior).nil?
          OpenApi::EnumValidator.validate("tax_behavior", _tax_behavior, POST_INVOICEITEMS_INVOICEITEM_VALID_VALUES_FOR_TAX_BEHAVIOR)
        end
        unless (_tax_code = tax_code).nil?
          OpenApi::EnumValidator.validate("tax_code", _tax_code, POST_INVOICEITEMS_INVOICEITEM_VALID_VALUES_FOR_TAX_CODE)
        end
        unless (_tax_rates = tax_rates).nil?
          _tax_rates.validate if _tax_rates.is_a?(OpenApi::Validatable)
        end
      end

      # resource path
      local_var_path = "/v1/invoiceitems/{invoiceitem}".sub("{" + "invoiceitem" + "}", URI.encode_path(invoiceitem.to_s))

      # header parameters
      header_params : Hash(String, String) = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])
      # HTTP header "Content-Type"
      header_params["Content-Type"] = @api_client.select_header_content_type(["application/x-www-form-urlencoded"])

      # cookie parameters
      cookie_params : Hash(String, String) = Hash(String, String).new

      # query parameters
      query_params : Hash(String, (String | Array(String) | JSON::Any)) = Hash(String, (String | Array(String) | JSON::Any)).new

      # form parameters
      form_params : Array(Tuple(String, Crest::ParamsValue)) | Nil = Array(Tuple(String, Crest::ParamsValue)).new
      form_params << Tuple(String, Crest::ParamsValue).new("amount", amount.to_s) if !amount.nil?
      form_params << Tuple(String, Crest::ParamsValue).new("description", description.to_s) if !description.nil?
      form_params << Tuple(String, Crest::ParamsValue).new("discountable", discountable.to_s) if !discountable.nil?
      form_params.concat(Crest::ZeroEnumeratedFlatParamsEncoder.flatten_params(JSON.parse(discounts.to_json), "discounts")) if !discounts.nil?
      form_params.concat(Crest::ZeroEnumeratedFlatParamsEncoder.flatten_params(JSON.parse(expand.to_json), "expand")) if !expand.nil?
      form_params.concat(Crest::ZeroEnumeratedFlatParamsEncoder.flatten_params(JSON.parse(metadata.to_json), "metadata")) if !metadata.nil?
      form_params.concat(Crest::ZeroEnumeratedFlatParamsEncoder.flatten_params(JSON.parse(period.to_json), "period")) if !period.nil?
      form_params << Tuple(String, Crest::ParamsValue).new("price", price.to_s) if !price.nil?
      form_params.concat(Crest::ZeroEnumeratedFlatParamsEncoder.flatten_params(JSON.parse(price_data.to_json), "price_data")) if !price_data.nil?
      form_params << Tuple(String, Crest::ParamsValue).new("quantity", quantity.to_s) if !quantity.nil?
      form_params << Tuple(String, Crest::ParamsValue).new("tax_behavior", tax_behavior.to_s) if !tax_behavior.nil?
      form_params << Tuple(String, Crest::ParamsValue).new("tax_code", tax_code.to_s) if !tax_code.nil?
      form_params.concat(Crest::ZeroEnumeratedFlatParamsEncoder.flatten_params(JSON.parse(tax_rates.to_json), "tax_rates")) if !tax_rates.nil?
      form_params << Tuple(String, Crest::ParamsValue).new("unit_amount", unit_amount.to_s) if !unit_amount.nil?
      form_params << Tuple(String, Crest::ParamsValue).new("unit_amount_decimal", unit_amount_decimal.to_s) if !unit_amount_decimal.nil?

      # http body (model)
      post_body : IO | String | Nil = nil

      # auth_names
      auth_names = ["basicAuth", "bearerAuth"]

      @api_client.build_api_request(
        http_method: :"POST",
        path: local_var_path,
        operation: "InvoiceitemsApi.post_invoiceitems_invoiceitem",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        cookie_params: cookie_params,
        query_params: query_params,
        form_params: form_params
      )
    end
  end
end
