#  Stripe API
#
# The Stripe REST API. Please see https://stripe.com/docs/api for more details.
#
# The version of the OpenAPI document: 2020-08-27
# Contact: dev-platform@stripe.com
# Generated by: https://openapi-generator.tech
#

require "nason"
require "time"
require "log"

module Stripe
  class PaymentIntentNextAction
    include NASON::Serializable

    # Required properties
    # Type of the next action to perform, one of `redirect_to_url`, `use_stripe_sdk`, `alipay_handle_redirect`, `oxxo_display_details`, or `verify_with_microdeposits`.
    @[NASON::Field(key: "type", type: String)]
    property _type : String

    # Optional properties
    @[NASON::Field(key: "alipay_handle_redirect", type: PaymentIntentNextActionAlipayHandleRedirect?, nillable: true)]
    property alipay_handle_redirect : PaymentIntentNextActionAlipayHandleRedirect?

    @[NASON::Field(key: "boleto_display_details", type: PaymentIntentNextActionBoleto?, nillable: true)]
    property boleto_display_details : PaymentIntentNextActionBoleto?

    @[NASON::Field(key: "oxxo_display_details", type: PaymentIntentNextActionDisplayOxxoDetails?, nillable: true)]
    property oxxo_display_details : PaymentIntentNextActionDisplayOxxoDetails?

    @[NASON::Field(key: "redirect_to_url", type: PaymentIntentNextActionRedirectToUrl?, nillable: true)]
    property redirect_to_url : PaymentIntentNextActionRedirectToUrl?

    # When confirming a PaymentIntent with Stripe.js, Stripe.js depends on the contents of this dictionary to invoke authentication flows. The shape of the contents is subject to change and is only intended to be used by Stripe.js.
    @[NASON::Field(key: "use_stripe_sdk", type: NASON::Any?, nillable: true)]
    property use_stripe_sdk : NASON::Any?

    @[NASON::Field(key: "verify_with_microdeposits", type: PaymentIntentNextActionVerifyWithMicrodeposits?, nillable: true)]
    property verify_with_microdeposits : PaymentIntentNextActionVerifyWithMicrodeposits?

    @[NASON::Field(key: "wechat_pay_display_qr_code", type: PaymentIntentNextActionWechatPayDisplayQrCode?, nillable: true)]
    property wechat_pay_display_qr_code : PaymentIntentNextActionWechatPayDisplayQrCode?

    @[NASON::Field(key: "wechat_pay_redirect_to_android_app", type: PaymentIntentNextActionWechatPayRedirectToAndroidApp?, nillable: true)]
    property wechat_pay_redirect_to_android_app : PaymentIntentNextActionWechatPayRedirectToAndroidApp?

    @[NASON::Field(key: "wechat_pay_redirect_to_ios_app", type: PaymentIntentNextActionWechatPayRedirectToIosApp?, nillable: true)]
    property wechat_pay_redirect_to_ios_app : PaymentIntentNextActionWechatPayRedirectToIosApp?

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(*, @_type : String, @alipay_handle_redirect : PaymentIntentNextActionAlipayHandleRedirect? = nil, @boleto_display_details : PaymentIntentNextActionBoleto? = nil, @oxxo_display_details : PaymentIntentNextActionDisplayOxxoDetails? = nil, @redirect_to_url : PaymentIntentNextActionRedirectToUrl? = nil, @use_stripe_sdk : NASON::Any? = nil, @verify_with_microdeposits : PaymentIntentNextActionVerifyWithMicrodeposits? = nil, @wechat_pay_display_qr_code : PaymentIntentNextActionWechatPayDisplayQrCode? = nil, @wechat_pay_redirect_to_android_app : PaymentIntentNextActionWechatPayRedirectToAndroidApp? = nil, @wechat_pay_redirect_to_ios_app : PaymentIntentNextActionWechatPayRedirectToIosApp? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if @_type.to_s.size > 5000
        invalid_properties.push("invalid value for \"_type\", the character length must be smaller than or equal to 5000.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @_type.to_s.size > 5000
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] _type Value to be assigned
    def _type=(_type)
      if _type.to_s.size > 5000
        raise ArgumentError.new("invalid value for \"_type\", the character length must be smaller than or equal to 5000.")
      end

      @_type = _type
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(o)
      return true if self.same?(o)
      self.class == o.class &&
        alipay_handle_redirect == o.alipay_handle_redirect &&
        boleto_display_details == o.boleto_display_details &&
        oxxo_display_details == o.oxxo_display_details &&
        redirect_to_url == o.redirect_to_url &&
        _type == o._type &&
        use_stripe_sdk == o.use_stripe_sdk &&
        verify_with_microdeposits == o.verify_with_microdeposits &&
        wechat_pay_display_qr_code == o.wechat_pay_display_qr_code &&
        wechat_pay_redirect_to_android_app == o.wechat_pay_redirect_to_android_app &&
        wechat_pay_redirect_to_ios_app == o.wechat_pay_redirect_to_ios_app
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(o)
      self == o
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [alipay_handle_redirect, boleto_display_details, oxxo_display_details, redirect_to_url, _type, use_stripe_sdk, verify_with_microdeposits, wechat_pay_display_qr_code, wechat_pay_redirect_to_android_app, wechat_pay_redirect_to_ios_app].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of String => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = Stripe.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {} of String => String
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.includes?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        ({} of String => String).tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end
  end
end
