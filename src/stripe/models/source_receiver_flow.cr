#  Stripe API
#
# The Stripe REST API. Please see https://stripe.com/docs/api for more details.
#
# The version of the OpenAPI document: 2020-08-27
# Contact: dev-platform@stripe.com
# Generated by: https://openapi-generator.tech
#

require "nason"
require "time"
require "log"

module Stripe
  class SourceReceiverFlow
    include NASON::Serializable

    # Required properties
    # The total amount that was moved to your balance. This is almost always equal to the amount charged. In rare cases when customers deposit excess funds and we are unable to refund those, those funds get moved to your balance and show up in amount_charged as well. The amount charged is expressed in the source's currency.
    @[NASON::Field(key: "amount_charged", type: Int64)]
    property amount_charged : Int64

    # The total amount received by the receiver source. `amount_received = amount_returned + amount_charged` should be true for consumed sources unless customers deposit excess funds. The amount received is expressed in the source's currency.
    @[NASON::Field(key: "amount_received", type: Int64)]
    property amount_received : Int64

    # The total amount that was returned to the customer. The amount returned is expressed in the source's currency.
    @[NASON::Field(key: "amount_returned", type: Int64)]
    property amount_returned : Int64

    # Type of refund attribute method, one of `email`, `manual`, or `none`.
    @[NASON::Field(key: "refund_attributes_method", type: String)]
    property refund_attributes_method : String

    # Type of refund attribute status, one of `missing`, `requested`, or `available`.
    @[NASON::Field(key: "refund_attributes_status", type: String)]
    property refund_attributes_status : String

    # Optional properties
    # The address of the receiver source. This is the value that should be communicated to the customer to send their funds to.
    @[NASON::Field(key: "address", type: String? | Null, nillable: true)]
    property address : String? | Null

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(*, @amount_charged : Int64, @amount_received : Int64, @amount_returned : Int64, @refund_attributes_method : String, @refund_attributes_status : String, @address : String? | Null = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if !@address.nil? && !@address.null? && @address.to_s.size > 5000
        invalid_properties.push("invalid value for \"address\", the character length must be smaller than or equal to 5000.")
      end

      if @refund_attributes_method.to_s.size > 5000
        invalid_properties.push("invalid value for \"refund_attributes_method\", the character length must be smaller than or equal to 5000.")
      end

      if @refund_attributes_status.to_s.size > 5000
        invalid_properties.push("invalid value for \"refund_attributes_status\", the character length must be smaller than or equal to 5000.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if !@address.nil? && !@address.null? && @address.to_s.size > 5000
      return false if @refund_attributes_method.to_s.size > 5000
      return false if @refund_attributes_status.to_s.size > 5000
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] address Value to be assigned
    def address=(address)
      if !address.nil? && !@address.null? && address.to_s.size > 5000
        raise ArgumentError.new("invalid value for \"address\", the character length must be smaller than or equal to 5000.")
      end

      @address = address
    end

    # Custom attribute writer method with validation
    # @param [Object] refund_attributes_method Value to be assigned
    def refund_attributes_method=(refund_attributes_method)
      if refund_attributes_method.to_s.size > 5000
        raise ArgumentError.new("invalid value for \"refund_attributes_method\", the character length must be smaller than or equal to 5000.")
      end

      @refund_attributes_method = refund_attributes_method
    end

    # Custom attribute writer method with validation
    # @param [Object] refund_attributes_status Value to be assigned
    def refund_attributes_status=(refund_attributes_status)
      if refund_attributes_status.to_s.size > 5000
        raise ArgumentError.new("invalid value for \"refund_attributes_status\", the character length must be smaller than or equal to 5000.")
      end

      @refund_attributes_status = refund_attributes_status
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(o)
      return true if self.same?(o)
      self.class == o.class &&
        address == o.address &&
        amount_charged == o.amount_charged &&
        amount_received == o.amount_received &&
        amount_returned == o.amount_returned &&
        refund_attributes_method == o.refund_attributes_method &&
        refund_attributes_status == o.refund_attributes_status
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(o)
      self == o
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [address, amount_charged, amount_received, amount_returned, refund_attributes_method, refund_attributes_status].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of String => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = Stripe.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {} of String => String
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.includes?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        ({} of String => String).tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end
  end
end
