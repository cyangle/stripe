#  Stripe API
#
# The Stripe REST API. Please see https://stripe.com/docs/api for more details.
#
# The version of the OpenAPI document: 2020-08-27
# Contact: dev-platform@stripe.com
# Generated by: https://openapi-generator.tech
#

require "nason"
require "time"
require "log"

module Stripe
  class IssuingTransactionFlightDataLeg
    include NASON::Serializable

    # Optional properties
    # The three-letter IATA airport code of the flight's destination.
    @[NASON::Field(key: "arrival_airport_code", type: String? | Null, nillable: true)]
    property arrival_airport_code : String? | Null

    # The airline carrier code.
    @[NASON::Field(key: "carrier", type: String? | Null, nillable: true)]
    property carrier : String? | Null

    # The three-letter IATA airport code that the flight departed from.
    @[NASON::Field(key: "departure_airport_code", type: String? | Null, nillable: true)]
    property departure_airport_code : String? | Null

    # The flight number.
    @[NASON::Field(key: "flight_number", type: String? | Null, nillable: true)]
    property flight_number : String? | Null

    # The flight's service class.
    @[NASON::Field(key: "service_class", type: String? | Null, nillable: true)]
    property service_class : String? | Null

    # Whether a stopover is allowed on this flight.
    @[NASON::Field(key: "stopover_allowed", type: Bool? | Null, nillable: true)]
    property stopover_allowed : Bool? | Null

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(*, @arrival_airport_code : String? | Null = nil, @carrier : String? | Null = nil, @departure_airport_code : String? | Null = nil, @flight_number : String? | Null = nil, @service_class : String? | Null = nil, @stopover_allowed : Bool? | Null = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if !@arrival_airport_code.nil? && !@arrival_airport_code.null? && @arrival_airport_code.to_s.size > 5000
        invalid_properties.push("invalid value for \"arrival_airport_code\", the character length must be smaller than or equal to 5000.")
      end

      if !@carrier.nil? && !@carrier.null? && @carrier.to_s.size > 5000
        invalid_properties.push("invalid value for \"carrier\", the character length must be smaller than or equal to 5000.")
      end

      if !@departure_airport_code.nil? && !@departure_airport_code.null? && @departure_airport_code.to_s.size > 5000
        invalid_properties.push("invalid value for \"departure_airport_code\", the character length must be smaller than or equal to 5000.")
      end

      if !@flight_number.nil? && !@flight_number.null? && @flight_number.to_s.size > 5000
        invalid_properties.push("invalid value for \"flight_number\", the character length must be smaller than or equal to 5000.")
      end

      if !@service_class.nil? && !@service_class.null? && @service_class.to_s.size > 5000
        invalid_properties.push("invalid value for \"service_class\", the character length must be smaller than or equal to 5000.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if !@arrival_airport_code.nil? && !@arrival_airport_code.null? && @arrival_airport_code.to_s.size > 5000
      return false if !@carrier.nil? && !@carrier.null? && @carrier.to_s.size > 5000
      return false if !@departure_airport_code.nil? && !@departure_airport_code.null? && @departure_airport_code.to_s.size > 5000
      return false if !@flight_number.nil? && !@flight_number.null? && @flight_number.to_s.size > 5000
      return false if !@service_class.nil? && !@service_class.null? && @service_class.to_s.size > 5000
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] arrival_airport_code Value to be assigned
    def arrival_airport_code=(arrival_airport_code)
      if !arrival_airport_code.nil? && !@arrival_airport_code.null? && arrival_airport_code.to_s.size > 5000
        raise ArgumentError.new("invalid value for \"arrival_airport_code\", the character length must be smaller than or equal to 5000.")
      end

      @arrival_airport_code = arrival_airport_code
    end

    # Custom attribute writer method with validation
    # @param [Object] carrier Value to be assigned
    def carrier=(carrier)
      if !carrier.nil? && !@carrier.null? && carrier.to_s.size > 5000
        raise ArgumentError.new("invalid value for \"carrier\", the character length must be smaller than or equal to 5000.")
      end

      @carrier = carrier
    end

    # Custom attribute writer method with validation
    # @param [Object] departure_airport_code Value to be assigned
    def departure_airport_code=(departure_airport_code)
      if !departure_airport_code.nil? && !@departure_airport_code.null? && departure_airport_code.to_s.size > 5000
        raise ArgumentError.new("invalid value for \"departure_airport_code\", the character length must be smaller than or equal to 5000.")
      end

      @departure_airport_code = departure_airport_code
    end

    # Custom attribute writer method with validation
    # @param [Object] flight_number Value to be assigned
    def flight_number=(flight_number)
      if !flight_number.nil? && !@flight_number.null? && flight_number.to_s.size > 5000
        raise ArgumentError.new("invalid value for \"flight_number\", the character length must be smaller than or equal to 5000.")
      end

      @flight_number = flight_number
    end

    # Custom attribute writer method with validation
    # @param [Object] service_class Value to be assigned
    def service_class=(service_class)
      if !service_class.nil? && !@service_class.null? && service_class.to_s.size > 5000
        raise ArgumentError.new("invalid value for \"service_class\", the character length must be smaller than or equal to 5000.")
      end

      @service_class = service_class
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(o)
      return true if self.same?(o)
      self.class == o.class &&
        arrival_airport_code == o.arrival_airport_code &&
        carrier == o.carrier &&
        departure_airport_code == o.departure_airport_code &&
        flight_number == o.flight_number &&
        service_class == o.service_class &&
        stopover_allowed == o.stopover_allowed
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(o)
      self == o
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [arrival_airport_code, carrier, departure_airport_code, flight_number, service_class, stopover_allowed].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of String => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = Stripe.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {} of String => String
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.includes?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        ({} of String => String).tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end
  end
end
