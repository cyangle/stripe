#  Stripe API
#
# The Stripe REST API. Please see https://stripe.com/docs/api for more details.
#
# The version of the OpenAPI document: 2020-08-27
# Contact: dev-platform@stripe.com
# Generated by: https://openapi-generator.tech
#

require "nason"
require "time"
require "log"

module Stripe
  class AccountBusinessProfile
    include NASON::Serializable

    # Optional properties
    # [The merchant category code for the account](https://stripe.com/docs/connect/setting-mcc). MCCs are used to classify businesses based on the goods or services they provide.
    @[NASON::Field(key: "mcc", type: String? | Null, nillable: true)]
    property mcc : String? | Null

    # The customer-facing business name.
    @[NASON::Field(key: "name", type: String? | Null, nillable: true)]
    property name : String? | Null

    # Internal-only description of the product sold or service provided by the business. It's used by Stripe for risk and underwriting purposes.
    @[NASON::Field(key: "product_description", type: String? | Null, nillable: true)]
    property product_description : String? | Null

    # A publicly available mailing address for sending support issues to.
    @[NASON::Field(key: "support_address", type: Address? | Null, nillable: true)]
    property support_address : Address? | Null

    # A publicly available email address for sending support issues to.
    @[NASON::Field(key: "support_email", type: String? | Null, nillable: true)]
    property support_email : String? | Null

    # A publicly available phone number to call with support issues.
    @[NASON::Field(key: "support_phone", type: String? | Null, nillable: true)]
    property support_phone : String? | Null

    # A publicly available website for handling support issues.
    @[NASON::Field(key: "support_url", type: String? | Null, nillable: true)]
    property support_url : String? | Null

    # The business's publicly available website.
    @[NASON::Field(key: "url", type: String? | Null, nillable: true)]
    property url : String? | Null

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(*, @mcc : String? | Null = nil, @name : String? | Null = nil, @product_description : String? | Null = nil, @support_address : Address? | Null = nil, @support_email : String? | Null = nil, @support_phone : String? | Null = nil, @support_url : String? | Null = nil, @url : String? | Null = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if !@mcc.nil? && !@mcc.null? && @mcc.to_s.size > 5000
        invalid_properties.push("invalid value for \"mcc\", the character length must be smaller than or equal to 5000.")
      end

      if !@name.nil? && !@name.null? && @name.to_s.size > 5000
        invalid_properties.push("invalid value for \"name\", the character length must be smaller than or equal to 5000.")
      end

      if !@product_description.nil? && !@product_description.null? && @product_description.to_s.size > 40000
        invalid_properties.push("invalid value for \"product_description\", the character length must be smaller than or equal to 40000.")
      end

      if !@support_email.nil? && !@support_email.null? && @support_email.to_s.size > 5000
        invalid_properties.push("invalid value for \"support_email\", the character length must be smaller than or equal to 5000.")
      end

      if !@support_phone.nil? && !@support_phone.null? && @support_phone.to_s.size > 5000
        invalid_properties.push("invalid value for \"support_phone\", the character length must be smaller than or equal to 5000.")
      end

      if !@support_url.nil? && !@support_url.null? && @support_url.to_s.size > 5000
        invalid_properties.push("invalid value for \"support_url\", the character length must be smaller than or equal to 5000.")
      end

      if !@url.nil? && !@url.null? && @url.to_s.size > 5000
        invalid_properties.push("invalid value for \"url\", the character length must be smaller than or equal to 5000.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if !@mcc.nil? && !@mcc.null? && @mcc.to_s.size > 5000
      return false if !@name.nil? && !@name.null? && @name.to_s.size > 5000
      return false if !@product_description.nil? && !@product_description.null? && @product_description.to_s.size > 40000
      return false if !@support_email.nil? && !@support_email.null? && @support_email.to_s.size > 5000
      return false if !@support_phone.nil? && !@support_phone.null? && @support_phone.to_s.size > 5000
      return false if !@support_url.nil? && !@support_url.null? && @support_url.to_s.size > 5000
      return false if !@url.nil? && !@url.null? && @url.to_s.size > 5000
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] mcc Value to be assigned
    def mcc=(mcc)
      if !mcc.nil? && !@mcc.null? && mcc.to_s.size > 5000
        raise ArgumentError.new("invalid value for \"mcc\", the character length must be smaller than or equal to 5000.")
      end

      @mcc = mcc
    end

    # Custom attribute writer method with validation
    # @param [Object] name Value to be assigned
    def name=(name)
      if !name.nil? && !@name.null? && name.to_s.size > 5000
        raise ArgumentError.new("invalid value for \"name\", the character length must be smaller than or equal to 5000.")
      end

      @name = name
    end

    # Custom attribute writer method with validation
    # @param [Object] product_description Value to be assigned
    def product_description=(product_description)
      if !product_description.nil? && !@product_description.null? && product_description.to_s.size > 40000
        raise ArgumentError.new("invalid value for \"product_description\", the character length must be smaller than or equal to 40000.")
      end

      @product_description = product_description
    end

    # Custom attribute writer method with validation
    # @param [Object] support_email Value to be assigned
    def support_email=(support_email)
      if !support_email.nil? && !@support_email.null? && support_email.to_s.size > 5000
        raise ArgumentError.new("invalid value for \"support_email\", the character length must be smaller than or equal to 5000.")
      end

      @support_email = support_email
    end

    # Custom attribute writer method with validation
    # @param [Object] support_phone Value to be assigned
    def support_phone=(support_phone)
      if !support_phone.nil? && !@support_phone.null? && support_phone.to_s.size > 5000
        raise ArgumentError.new("invalid value for \"support_phone\", the character length must be smaller than or equal to 5000.")
      end

      @support_phone = support_phone
    end

    # Custom attribute writer method with validation
    # @param [Object] support_url Value to be assigned
    def support_url=(support_url)
      if !support_url.nil? && !@support_url.null? && support_url.to_s.size > 5000
        raise ArgumentError.new("invalid value for \"support_url\", the character length must be smaller than or equal to 5000.")
      end

      @support_url = support_url
    end

    # Custom attribute writer method with validation
    # @param [Object] url Value to be assigned
    def url=(url)
      if !url.nil? && !@url.null? && url.to_s.size > 5000
        raise ArgumentError.new("invalid value for \"url\", the character length must be smaller than or equal to 5000.")
      end

      @url = url
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(o)
      return true if self.same?(o)
      self.class == o.class &&
        mcc == o.mcc &&
        name == o.name &&
        product_description == o.product_description &&
        support_address == o.support_address &&
        support_email == o.support_email &&
        support_phone == o.support_phone &&
        support_url == o.support_url &&
        url == o.url
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(o)
      self == o
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [mcc, name, product_description, support_address, support_email, support_phone, support_url, url].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of String => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = Stripe.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {} of String => String
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.includes?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        ({} of String => String).tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end
  end
end
