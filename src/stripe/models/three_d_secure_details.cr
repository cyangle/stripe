#  Stripe API
#
# The Stripe REST API. Please see https://stripe.com/docs/api for more details.
#
# The version of the OpenAPI document: 2020-08-27
# Contact: dev-platform@stripe.com
# Generated by: https://openapi-generator.tech
#

require "nason"
require "time"
require "log"

module Stripe
  class ThreeDSecureDetails
    include NASON::Serializable

    # Optional properties
    # For authenticated transactions: how the customer was authenticated by the issuing bank.
    @[NASON::Field(key: "authentication_flow", type: String? | Null, nillable: true)]
    property authentication_flow : String? | Null

    # Indicates the outcome of 3D Secure authentication.
    @[NASON::Field(key: "result", type: String? | Null, nillable: true)]
    property result : String? | Null

    # Additional information about why 3D Secure succeeded or failed based on the `result`.
    @[NASON::Field(key: "result_reason", type: String? | Null, nillable: true)]
    property result_reason : String? | Null

    # The version of 3D Secure that was used.
    @[NASON::Field(key: "version", type: String? | Null, nillable: true)]
    property version : String? | Null

    class EnumAttributeValidator
      getter datatype : String
      getter allowable_values : Array(String | Int64 | Float64)

      def initialize(datatype, allowable_values)
        @datatype = datatype
        @allowable_values = allowable_values.map do |value|
          case datatype.to_s
          when /Integer/i
            value.to_i
          when /Float/i
            value.to_f
          else
            value
          end
        end
      end

      def valid?(value)
        value.nil? || value.null? || allowable_values.includes?(value)
      end
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(*, @authentication_flow : String? | Null = nil, @result : String? | Null = nil, @result_reason : String? | Null = nil, @version : String? | Null = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      authentication_flow_validator = EnumAttributeValidator.new("String", ["challenge", "frictionless"])
      return false unless authentication_flow_validator.valid?(@authentication_flow)
      result_validator = EnumAttributeValidator.new("String", ["attempt_acknowledged", "authenticated", "failed", "not_supported", "processing_error"])
      return false unless result_validator.valid?(@result)
      result_reason_validator = EnumAttributeValidator.new("String", ["abandoned", "bypassed", "canceled", "card_not_enrolled", "network_not_supported", "protocol_error", "rejected"])
      return false unless result_reason_validator.valid?(@result_reason)
      version_validator = EnumAttributeValidator.new("String", ["1.0.2", "2.1.0", "2.2.0"])
      return false unless version_validator.valid?(@version)
      true
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] authentication_flow Object to be assigned
    def authentication_flow=(authentication_flow)
      validator = EnumAttributeValidator.new("String", ["challenge", "frictionless"])
      unless validator.valid?(authentication_flow)
        raise ArgumentError.new("invalid value for \"authentication_flow\", must be one of #{validator.allowable_values}.")
      end
      @authentication_flow = authentication_flow
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] result Object to be assigned
    def result=(result)
      validator = EnumAttributeValidator.new("String", ["attempt_acknowledged", "authenticated", "failed", "not_supported", "processing_error"])
      unless validator.valid?(result)
        raise ArgumentError.new("invalid value for \"result\", must be one of #{validator.allowable_values}.")
      end
      @result = result
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] result_reason Object to be assigned
    def result_reason=(result_reason)
      validator = EnumAttributeValidator.new("String", ["abandoned", "bypassed", "canceled", "card_not_enrolled", "network_not_supported", "protocol_error", "rejected"])
      unless validator.valid?(result_reason)
        raise ArgumentError.new("invalid value for \"result_reason\", must be one of #{validator.allowable_values}.")
      end
      @result_reason = result_reason
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] version Object to be assigned
    def version=(version)
      validator = EnumAttributeValidator.new("String", ["1.0.2", "2.1.0", "2.2.0"])
      unless validator.valid?(version)
        raise ArgumentError.new("invalid value for \"version\", must be one of #{validator.allowable_values}.")
      end
      @version = version
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(o)
      return true if self.same?(o)
      self.class == o.class &&
        authentication_flow == o.authentication_flow &&
        result == o.result &&
        result_reason == o.result_reason &&
        version == o.version
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(o)
      self == o
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [authentication_flow, result, result_reason, version].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of String => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = Stripe.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {} of String => String
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.includes?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        ({} of String => String).tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end
  end
end
