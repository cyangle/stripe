#  Stripe API
#
# The Stripe REST API. Please see https://stripe.com/docs/api for more details.
#
# The version of the OpenAPI document: 2020-08-27
# Contact: dev-platform@stripe.com
# Generated by: https://openapi-generator.tech
#

require "nason"
require "time"
require "log"

module Stripe
  # Invoices are statements of amounts owed by a customer, and are either generated one-off, or generated periodically from a subscription.  They contain [invoice items](https://stripe.com/docs/api#invoiceitems), and proration adjustments that may be caused by subscription upgrades/downgrades (if necessary).  If your invoice is configured to be billed through automatic charges, Stripe automatically finalizes your invoice and attempts payment. Note that finalizing the invoice, [when automatic](https://stripe.com/docs/billing/invoices/workflow/#auto_advance), does not happen immediately as the invoice is created. Stripe waits until one hour after the last webhook was successfully sent (or the last webhook timed out after failing). If you (and the platforms you may have connected to) have no webhooks configured, Stripe waits one hour after creation to finalize the invoice.  If your invoice is configured to be billed by sending an email, then based on your [email settings](https://dashboard.stripe.com/account/billing/automatic), Stripe will email the invoice to your customer and await payment. These emails can contain a link to a hosted page to pay the invoice.  Stripe applies any customer credit on the account before determining the amount due for the invoice (i.e., the amount that will be actually charged). If the amount due for the invoice is less than Stripe's [minimum allowed charge per currency](/docs/currencies#minimum-and-maximum-charge-amounts), the invoice is automatically marked paid, and we add the amount due to the customer's credit balance which is applied to the next invoice.  More details on the customer's credit balance are [here](https://stripe.com/docs/billing/customer/balance).  Related guide: [Send Invoices to Customers](https://stripe.com/docs/billing/invoices/sending).
  class Invoice
    include NASON::Serializable

    # Required properties
    # Final amount due at this time for this invoice. If the invoice's total is smaller than the minimum charge amount, for example, or if there is account credit that can be applied to the invoice, the `amount_due` may be 0. If there is a positive `starting_balance` for the invoice (the customer owes money), the `amount_due` will also take that into account. The charge that gets generated for the invoice will be for the amount specified in `amount_due`.
    @[NASON::Field(key: "amount_due", type: Int64)]
    property amount_due : Int64

    # The amount, in %s, that was paid.
    @[NASON::Field(key: "amount_paid", type: Int64)]
    property amount_paid : Int64

    # The amount remaining, in %s, that is due.
    @[NASON::Field(key: "amount_remaining", type: Int64)]
    property amount_remaining : Int64

    # Number of payment attempts made for this invoice, from the perspective of the payment retry schedule. Any payment attempt counts as the first attempt, and subsequently only automatic retries increment the attempt count. In other words, manual payment attempts after the first attempt do not affect the retry schedule.
    @[NASON::Field(key: "attempt_count", type: Int64)]
    property attempt_count : Int64

    # Whether an attempt has been made to pay the invoice. An invoice is not attempted until 1 hour after the `invoice.created` webhook, for example, so you might not want to display that invoice as unpaid to your users.
    @[NASON::Field(key: "attempted", type: Bool)]
    property attempted : Bool

    @[NASON::Field(key: "automatic_tax", type: AutomaticTax)]
    property automatic_tax : AutomaticTax

    # Either `charge_automatically`, or `send_invoice`. When charging automatically, Stripe will attempt to pay this invoice using the default source attached to the customer. When sending an invoice, Stripe will email this invoice to the customer with payment instructions.
    @[NASON::Field(key: "collection_method", type: String)]
    property collection_method : String

    # Time at which the object was created. Measured in seconds since the Unix epoch.
    @[NASON::Field(key: "created", type: Int64)]
    property created : Int64

    # Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
    @[NASON::Field(key: "currency", type: String)]
    property currency : String

    # The tax rates applied to this invoice, if any.
    @[NASON::Field(key: "default_tax_rates", type: Array(TaxRate))]
    property default_tax_rates : Array(TaxRate)

    @[NASON::Field(key: "lines", type: InvoiceLinesList1)]
    property lines : InvoiceLinesList1

    # Has the value `true` if the object exists in live mode or the value `false` if the object exists in test mode.
    @[NASON::Field(key: "livemode", type: Bool)]
    property livemode : Bool

    # String representing the object's type. Objects of the same type share the same value.
    @[NASON::Field(key: "object", type: String)]
    property object : String

    # Whether payment was successfully collected for this invoice. An invoice can be paid (most commonly) with a charge or with credit from the customer's account balance.
    @[NASON::Field(key: "paid", type: Bool)]
    property paid : Bool

    # Returns true if the invoice was manually marked paid, returns false if the invoice hasn't been paid yet or was paid on Stripe.
    @[NASON::Field(key: "paid_out_of_band", type: Bool)]
    property paid_out_of_band : Bool

    @[NASON::Field(key: "payment_settings", type: InvoicesPaymentSettings)]
    property payment_settings : InvoicesPaymentSettings

    # End of the usage period during which invoice items were added to this invoice.
    @[NASON::Field(key: "period_end", type: Int64)]
    property period_end : Int64

    # Start of the usage period during which invoice items were added to this invoice.
    @[NASON::Field(key: "period_start", type: Int64)]
    property period_start : Int64

    # Total amount of all post-payment credit notes issued for this invoice.
    @[NASON::Field(key: "post_payment_credit_notes_amount", type: Int64)]
    property post_payment_credit_notes_amount : Int64

    # Total amount of all pre-payment credit notes issued for this invoice.
    @[NASON::Field(key: "pre_payment_credit_notes_amount", type: Int64)]
    property pre_payment_credit_notes_amount : Int64

    # Starting customer balance before the invoice is finalized. If the invoice has not been finalized yet, this will be the current customer balance.
    @[NASON::Field(key: "starting_balance", type: Int64)]
    property starting_balance : Int64

    @[NASON::Field(key: "status_transitions", type: InvoicesStatusTransitions)]
    property status_transitions : InvoicesStatusTransitions

    # Total of all subscriptions, invoice items, and prorations on the invoice before any invoice level discount or tax is applied. Item discounts are already incorporated
    @[NASON::Field(key: "subtotal", type: Int64)]
    property subtotal : Int64

    # Total after discounts and taxes.
    @[NASON::Field(key: "total", type: Int64)]
    property total : Int64

    # The aggregate amounts calculated per tax rate for all line items.
    @[NASON::Field(key: "total_tax_amounts", type: Array(InvoiceTaxAmount))]
    property total_tax_amounts : Array(InvoiceTaxAmount)

    # Optional properties
    # The country of the business associated with this invoice, most often the business creating the invoice.
    @[NASON::Field(key: "account_country", type: String? | Null, nillable: true)]
    property account_country : String? | Null

    # The public name of the business associated with this invoice, most often the business creating the invoice.
    @[NASON::Field(key: "account_name", type: String? | Null, nillable: true)]
    property account_name : String? | Null

    # The account tax IDs associated with the invoice. Only editable when the invoice is a draft.
    @[NASON::Field(key: "account_tax_ids", type: Array(String | TaxId)? | Null, nillable: true)]
    property account_tax_ids : Array(String | TaxId)? | Null

    # The fee in %s that will be applied to the invoice and transferred to the application owner's Stripe account when the invoice is paid.
    @[NASON::Field(key: "application_fee_amount", type: Int64? | Null, nillable: true)]
    property application_fee_amount : Int64? | Null

    # Controls whether Stripe will perform [automatic collection](https://stripe.com/docs/billing/invoices/workflow/#auto_advance) of the invoice. When `false`, the invoice's state will not automatically advance without an explicit action.
    @[NASON::Field(key: "auto_advance", type: Bool?, nillable: true)]
    property auto_advance : Bool?

    # Indicates the reason why the invoice was created. `subscription_cycle` indicates an invoice created by a subscription advancing into a new period. `subscription_create` indicates an invoice created due to creating a subscription. `subscription_update` indicates an invoice created due to updating a subscription. `subscription` is set for all old invoices to indicate either a change to a subscription or a period advancement. `manual` is set for all invoices unrelated to a subscription (for example: created via the invoice editor). The `upcoming` value is reserved for simulated invoices per the upcoming invoice endpoint. `subscription_threshold` indicates an invoice created due to a billing threshold being reached.
    @[NASON::Field(key: "billing_reason", type: String? | Null, nillable: true)]
    property billing_reason : String? | Null

    # ID of the latest charge generated for this invoice, if any.
    @[NASON::Field(key: "charge", type: String | Charge? | Null, nillable: true)]
    property charge : String | Charge? | Null

    # Custom fields displayed on the invoice.
    @[NASON::Field(key: "custom_fields", type: Array(InvoiceSettingCustomField)? | Null, nillable: true)]
    property custom_fields : Array(InvoiceSettingCustomField)? | Null

    # The ID of the customer who will be billed.
    @[NASON::Field(key: "customer", type: String | Customer | Nil | Null, nillable: true)]
    property customer : String | Customer | Nil | Null

    # The customer's address. Until the invoice is finalized, this field will equal `customer.address`. Once the invoice is finalized, this field will no longer be updated.
    @[NASON::Field(key: "customer_address", type: Address? | Null, nillable: true)]
    property customer_address : Address? | Null

    # The customer's email. Until the invoice is finalized, this field will equal `customer.email`. Once the invoice is finalized, this field will no longer be updated.
    @[NASON::Field(key: "customer_email", type: String? | Null, nillable: true)]
    property customer_email : String? | Null

    # The customer's name. Until the invoice is finalized, this field will equal `customer.name`. Once the invoice is finalized, this field will no longer be updated.
    @[NASON::Field(key: "customer_name", type: String? | Null, nillable: true)]
    property customer_name : String? | Null

    # The customer's phone number. Until the invoice is finalized, this field will equal `customer.phone`. Once the invoice is finalized, this field will no longer be updated.
    @[NASON::Field(key: "customer_phone", type: String? | Null, nillable: true)]
    property customer_phone : String? | Null

    # The customer's shipping information. Until the invoice is finalized, this field will equal `customer.shipping`. Once the invoice is finalized, this field will no longer be updated.
    @[NASON::Field(key: "customer_shipping", type: Shipping? | Null, nillable: true)]
    property customer_shipping : Shipping? | Null

    # The customer's tax exempt status. Until the invoice is finalized, this field will equal `customer.tax_exempt`. Once the invoice is finalized, this field will no longer be updated.
    @[NASON::Field(key: "customer_tax_exempt", type: String? | Null, nillable: true)]
    property customer_tax_exempt : String? | Null

    # The customer's tax IDs. Until the invoice is finalized, this field will contain the same tax IDs as `customer.tax_ids`. Once the invoice is finalized, this field will no longer be updated.
    @[NASON::Field(key: "customer_tax_ids", type: Array(InvoicesResourceInvoiceTaxId)? | Null, nillable: true)]
    property customer_tax_ids : Array(InvoicesResourceInvoiceTaxId)? | Null

    # ID of the default payment method for the invoice. It must belong to the customer associated with the invoice. If not set, defaults to the subscription's default payment method, if any, or to the default payment method in the customer's invoice settings.
    @[NASON::Field(key: "default_payment_method", type: String | PaymentMethod? | Null, nillable: true)]
    property default_payment_method : String | PaymentMethod? | Null

    # ID of the default payment source for the invoice. It must belong to the customer associated with the invoice and be in a chargeable state. If not set, defaults to the subscription's default source, if any, or to the customer's default source.
    @[NASON::Field(key: "default_source", type: String | AlipayAccount | BankAccount | BitcoinReceiver | Card | Source? | Null, nillable: true)]
    property default_source : String | AlipayAccount | BankAccount | BitcoinReceiver | Card | Source? | Null

    # An arbitrary string attached to the object. Often useful for displaying to users. Referenced as 'memo' in the Dashboard.
    @[NASON::Field(key: "description", type: String? | Null, nillable: true)]
    property description : String? | Null

    # Describes the current discount applied to this invoice, if there is one. Not populated if there are multiple discounts.
    @[NASON::Field(key: "discount", type: Discount? | Null, nillable: true)]
    property discount : Discount? | Null

    # The discounts applied to the invoice. Line item discounts are applied before invoice discounts. Use `expand[]=discounts` to expand each discount.
    @[NASON::Field(key: "discounts", type: Array(String | Discount)? | Null, nillable: true)]
    property discounts : Array(String | Discount)? | Null

    # The date on which payment for this invoice is due. This value will be `null` for invoices where `collection_method=charge_automatically`.
    @[NASON::Field(key: "due_date", type: Int64? | Null, nillable: true)]
    property due_date : Int64? | Null

    # Ending customer balance after the invoice is finalized. Invoices are finalized approximately an hour after successful webhook delivery or when payment collection is attempted for the invoice. If the invoice has not been finalized yet, this will be null.
    @[NASON::Field(key: "ending_balance", type: Int64? | Null, nillable: true)]
    property ending_balance : Int64? | Null

    # Footer displayed on the invoice.
    @[NASON::Field(key: "footer", type: String? | Null, nillable: true)]
    property footer : String? | Null

    # The URL for the hosted invoice page, which allows customers to view and pay an invoice. If the invoice has not been finalized yet, this will be null.
    @[NASON::Field(key: "hosted_invoice_url", type: String? | Null, nillable: true)]
    property hosted_invoice_url : String? | Null

    # Unique identifier for the object.
    @[NASON::Field(key: "id", type: String?, nillable: true)]
    property id : String?

    # The link to download the PDF for the invoice. If the invoice has not been finalized yet, this will be null.
    @[NASON::Field(key: "invoice_pdf", type: String? | Null, nillable: true)]
    property invoice_pdf : String? | Null

    # The error encountered during the previous attempt to finalize the invoice. This field is cleared when the invoice is successfully finalized.
    @[NASON::Field(key: "last_finalization_error", type: ApiErrors? | Null, nillable: true)]
    property last_finalization_error : ApiErrors? | Null

    # Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
    @[NASON::Field(key: "metadata", type: Hash(String, String)? | Null, nillable: true)]
    property metadata : Hash(String, String)? | Null

    # The time at which payment will next be attempted. This value will be `null` for invoices where `collection_method=send_invoice`.
    @[NASON::Field(key: "next_payment_attempt", type: Int64? | Null, nillable: true)]
    property next_payment_attempt : Int64? | Null

    # A unique, identifying string that appears on emails sent to the customer for this invoice. This starts with the customer's unique invoice_prefix if it is specified.
    @[NASON::Field(key: "number", type: String? | Null, nillable: true)]
    property number : String? | Null

    # The account (if any) for which the funds of the invoice payment are intended. If set, the invoice will be presented with the branding and support information of the specified account. See the [Invoices with Connect](https://stripe.com/docs/billing/invoices/connect) documentation for details.
    @[NASON::Field(key: "on_behalf_of", type: String | Account? | Null, nillable: true)]
    property on_behalf_of : String | Account? | Null

    # The PaymentIntent associated with this invoice. The PaymentIntent is generated when the invoice is finalized, and can then be used to pay the invoice. Note that voiding an invoice will cancel the PaymentIntent.
    @[NASON::Field(key: "payment_intent", type: String | PaymentIntent? | Null, nillable: true)]
    property payment_intent : String | PaymentIntent? | Null

    # The quote this invoice was generated from.
    @[NASON::Field(key: "quote", type: String | Quote? | Null, nillable: true)]
    property quote : String | Quote? | Null

    # This is the transaction number that appears on email receipts sent for this invoice.
    @[NASON::Field(key: "receipt_number", type: String? | Null, nillable: true)]
    property receipt_number : String? | Null

    # Extra information about an invoice for the customer's credit card statement.
    @[NASON::Field(key: "statement_descriptor", type: String? | Null, nillable: true)]
    property statement_descriptor : String? | Null

    # The status of the invoice, one of `draft`, `open`, `paid`, `uncollectible`, or `void`. [Learn more](https://stripe.com/docs/billing/invoices/workflow#workflow-overview)
    @[NASON::Field(key: "status", type: String? | Null, nillable: true)]
    property status : String? | Null

    # The subscription that this invoice was prepared for, if any.
    @[NASON::Field(key: "subscription", type: String | Subscription? | Null, nillable: true)]
    property subscription : String | Subscription? | Null

    # Only set for upcoming invoices that preview prorations. The time used to calculate prorations.
    @[NASON::Field(key: "subscription_proration_date", type: Int64?, nillable: true)]
    property subscription_proration_date : Int64?

    # The amount of tax on this invoice. This is the sum of all the tax amounts on this invoice.
    @[NASON::Field(key: "tax", type: Int64? | Null, nillable: true)]
    property tax : Int64? | Null

    @[NASON::Field(key: "threshold_reason", type: InvoiceThresholdReason?, nillable: true)]
    property threshold_reason : InvoiceThresholdReason?

    # The aggregate amounts calculated per discount across all line items.
    @[NASON::Field(key: "total_discount_amounts", type: Array(DiscountsResourceDiscountAmount)? | Null, nillable: true)]
    property total_discount_amounts : Array(DiscountsResourceDiscountAmount)? | Null

    # The account (if any) the payment will be attributed to for tax reporting, and where funds from the payment will be transferred to for the invoice.
    @[NASON::Field(key: "transfer_data", type: InvoiceTransferData? | Null, nillable: true)]
    property transfer_data : InvoiceTransferData? | Null

    # Invoices are automatically paid or sent 1 hour after webhooks are delivered, or until all webhook delivery attempts have [been exhausted](https://stripe.com/docs/billing/webhooks#understand). This field tracks the time when webhooks for this invoice were successfully delivered. If the invoice had no webhooks to deliver, this will be set while the invoice is being created.
    @[NASON::Field(key: "webhooks_delivered_at", type: Int64? | Null, nillable: true)]
    property webhooks_delivered_at : Int64? | Null

    class EnumAttributeValidator
      getter datatype : String
      getter allowable_values : Array(String | Int64 | Float64)

      def initialize(datatype, allowable_values)
        @datatype = datatype
        @allowable_values = allowable_values.map do |value|
          case datatype.to_s
          when /Integer/i
            value.to_i
          when /Float/i
            value.to_f
          else
            value
          end
        end
      end

      def valid?(value)
        value.nil? || value.null? || allowable_values.includes?(value)
      end
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(*, @amount_due : Int64, @amount_paid : Int64, @amount_remaining : Int64, @attempt_count : Int64, @attempted : Bool, @automatic_tax : AutomaticTax, @collection_method : String, @created : Int64, @currency : String, @default_tax_rates : Array(TaxRate), @lines : InvoiceLinesList1, @livemode : Bool, @object : String, @paid : Bool, @paid_out_of_band : Bool, @payment_settings : InvoicesPaymentSettings, @period_end : Int64, @period_start : Int64, @post_payment_credit_notes_amount : Int64, @pre_payment_credit_notes_amount : Int64, @starting_balance : Int64, @status_transitions : InvoicesStatusTransitions, @subtotal : Int64, @total : Int64, @total_tax_amounts : Array(InvoiceTaxAmount), @account_country : String? | Null = nil, @account_name : String? | Null = nil, @account_tax_ids : Array(String | TaxId)? | Null = nil, @application_fee_amount : Int64? | Null = nil, @auto_advance : Bool? = nil, @billing_reason : String? | Null = nil, @charge : String | Charge? | Null = nil, @custom_fields : Array(InvoiceSettingCustomField)? | Null = nil, @customer : String | Customer | Nil | Null = nil, @customer_address : Address? | Null = nil, @customer_email : String? | Null = nil, @customer_name : String? | Null = nil, @customer_phone : String? | Null = nil, @customer_shipping : Shipping? | Null = nil, @customer_tax_exempt : String? | Null = nil, @customer_tax_ids : Array(InvoicesResourceInvoiceTaxId)? | Null = nil, @default_payment_method : String | PaymentMethod? | Null = nil, @default_source : String | AlipayAccount | BankAccount | BitcoinReceiver | Card | Source? | Null = nil, @description : String? | Null = nil, @discount : Discount? | Null = nil, @discounts : Array(String | Discount)? | Null = nil, @due_date : Int64? | Null = nil, @ending_balance : Int64? | Null = nil, @footer : String? | Null = nil, @hosted_invoice_url : String? | Null = nil, @id : String? = nil, @invoice_pdf : String? | Null = nil, @last_finalization_error : ApiErrors? | Null = nil, @metadata : Hash(String, String)? | Null = nil, @next_payment_attempt : Int64? | Null = nil, @number : String? | Null = nil, @on_behalf_of : String | Account? | Null = nil, @payment_intent : String | PaymentIntent? | Null = nil, @quote : String | Quote? | Null = nil, @receipt_number : String? | Null = nil, @statement_descriptor : String? | Null = nil, @status : String? | Null = nil, @subscription : String | Subscription? | Null = nil, @subscription_proration_date : Int64? = nil, @tax : Int64? | Null = nil, @threshold_reason : InvoiceThresholdReason? = nil, @total_discount_amounts : Array(DiscountsResourceDiscountAmount)? | Null = nil, @transfer_data : InvoiceTransferData? | Null = nil, @webhooks_delivered_at : Int64? | Null = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if !@account_country.nil? && !@account_country.null? && @account_country.to_s.size > 5000
        invalid_properties.push("invalid value for \"account_country\", the character length must be smaller than or equal to 5000.")
      end

      if !@account_name.nil? && !@account_name.null? && @account_name.to_s.size > 5000
        invalid_properties.push("invalid value for \"account_name\", the character length must be smaller than or equal to 5000.")
      end

      if !@customer_email.nil? && !@customer_email.null? && @customer_email.to_s.size > 5000
        invalid_properties.push("invalid value for \"customer_email\", the character length must be smaller than or equal to 5000.")
      end

      if !@customer_name.nil? && !@customer_name.null? && @customer_name.to_s.size > 5000
        invalid_properties.push("invalid value for \"customer_name\", the character length must be smaller than or equal to 5000.")
      end

      if !@customer_phone.nil? && !@customer_phone.null? && @customer_phone.to_s.size > 5000
        invalid_properties.push("invalid value for \"customer_phone\", the character length must be smaller than or equal to 5000.")
      end

      if !@description.nil? && !@description.null? && @description.to_s.size > 5000
        invalid_properties.push("invalid value for \"description\", the character length must be smaller than or equal to 5000.")
      end

      if !@footer.nil? && !@footer.null? && @footer.to_s.size > 5000
        invalid_properties.push("invalid value for \"footer\", the character length must be smaller than or equal to 5000.")
      end

      if !@hosted_invoice_url.nil? && !@hosted_invoice_url.null? && @hosted_invoice_url.to_s.size > 5000
        invalid_properties.push("invalid value for \"hosted_invoice_url\", the character length must be smaller than or equal to 5000.")
      end

      if !@id.nil? && @id.to_s.size > 5000
        invalid_properties.push("invalid value for \"id\", the character length must be smaller than or equal to 5000.")
      end

      if !@invoice_pdf.nil? && !@invoice_pdf.null? && @invoice_pdf.to_s.size > 5000
        invalid_properties.push("invalid value for \"invoice_pdf\", the character length must be smaller than or equal to 5000.")
      end

      if !@number.nil? && !@number.null? && @number.to_s.size > 5000
        invalid_properties.push("invalid value for \"number\", the character length must be smaller than or equal to 5000.")
      end

      if !@receipt_number.nil? && !@receipt_number.null? && @receipt_number.to_s.size > 5000
        invalid_properties.push("invalid value for \"receipt_number\", the character length must be smaller than or equal to 5000.")
      end

      if !@statement_descriptor.nil? && !@statement_descriptor.null? && @statement_descriptor.to_s.size > 5000
        invalid_properties.push("invalid value for \"statement_descriptor\", the character length must be smaller than or equal to 5000.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if !@account_country.nil? && !@account_country.null? && @account_country.to_s.size > 5000
      return false if !@account_name.nil? && !@account_name.null? && @account_name.to_s.size > 5000
      billing_reason_validator = EnumAttributeValidator.new("String", ["automatic_pending_invoice_item_invoice", "manual", "quote_accept", "subscription", "subscription_create", "subscription_cycle", "subscription_threshold", "subscription_update", "upcoming"])
      return false unless billing_reason_validator.valid?(@billing_reason)
      collection_method_validator = EnumAttributeValidator.new("String", ["charge_automatically", "send_invoice"])
      return false unless collection_method_validator.valid?(@collection_method)
      return false if !@customer_email.nil? && !@customer_email.null? && @customer_email.to_s.size > 5000
      return false if !@customer_name.nil? && !@customer_name.null? && @customer_name.to_s.size > 5000
      return false if !@customer_phone.nil? && !@customer_phone.null? && @customer_phone.to_s.size > 5000
      customer_tax_exempt_validator = EnumAttributeValidator.new("String", ["exempt", "none", "reverse"])
      return false unless customer_tax_exempt_validator.valid?(@customer_tax_exempt)
      return false if !@description.nil? && !@description.null? && @description.to_s.size > 5000
      return false if !@footer.nil? && !@footer.null? && @footer.to_s.size > 5000
      return false if !@hosted_invoice_url.nil? && !@hosted_invoice_url.null? && @hosted_invoice_url.to_s.size > 5000
      return false if !@id.nil? && @id.to_s.size > 5000
      return false if !@invoice_pdf.nil? && !@invoice_pdf.null? && @invoice_pdf.to_s.size > 5000
      return false if !@number.nil? && !@number.null? && @number.to_s.size > 5000
      object_validator = EnumAttributeValidator.new("String", ["invoice"])
      return false unless object_validator.valid?(@object)
      return false if !@receipt_number.nil? && !@receipt_number.null? && @receipt_number.to_s.size > 5000
      return false if !@statement_descriptor.nil? && !@statement_descriptor.null? && @statement_descriptor.to_s.size > 5000
      status_validator = EnumAttributeValidator.new("String", ["deleted", "draft", "open", "paid", "uncollectible", "void"])
      return false unless status_validator.valid?(@status)
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] account_country Value to be assigned
    def account_country=(account_country)
      if !account_country.nil? && !@account_country.null? && account_country.to_s.size > 5000
        raise ArgumentError.new("invalid value for \"account_country\", the character length must be smaller than or equal to 5000.")
      end

      @account_country = account_country
    end

    # Custom attribute writer method with validation
    # @param [Object] account_name Value to be assigned
    def account_name=(account_name)
      if !account_name.nil? && !@account_name.null? && account_name.to_s.size > 5000
        raise ArgumentError.new("invalid value for \"account_name\", the character length must be smaller than or equal to 5000.")
      end

      @account_name = account_name
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] billing_reason Object to be assigned
    def billing_reason=(billing_reason)
      validator = EnumAttributeValidator.new("String", ["automatic_pending_invoice_item_invoice", "manual", "quote_accept", "subscription", "subscription_create", "subscription_cycle", "subscription_threshold", "subscription_update", "upcoming"])
      unless validator.valid?(billing_reason)
        raise ArgumentError.new("invalid value for \"billing_reason\", must be one of #{validator.allowable_values}.")
      end
      @billing_reason = billing_reason
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] collection_method Object to be assigned
    def collection_method=(collection_method)
      validator = EnumAttributeValidator.new("String", ["charge_automatically", "send_invoice"])
      unless validator.valid?(collection_method)
        raise ArgumentError.new("invalid value for \"collection_method\", must be one of #{validator.allowable_values}.")
      end
      @collection_method = collection_method
    end

    # Custom attribute writer method with validation
    # @param [Object] customer_email Value to be assigned
    def customer_email=(customer_email)
      if !customer_email.nil? && !@customer_email.null? && customer_email.to_s.size > 5000
        raise ArgumentError.new("invalid value for \"customer_email\", the character length must be smaller than or equal to 5000.")
      end

      @customer_email = customer_email
    end

    # Custom attribute writer method with validation
    # @param [Object] customer_name Value to be assigned
    def customer_name=(customer_name)
      if !customer_name.nil? && !@customer_name.null? && customer_name.to_s.size > 5000
        raise ArgumentError.new("invalid value for \"customer_name\", the character length must be smaller than or equal to 5000.")
      end

      @customer_name = customer_name
    end

    # Custom attribute writer method with validation
    # @param [Object] customer_phone Value to be assigned
    def customer_phone=(customer_phone)
      if !customer_phone.nil? && !@customer_phone.null? && customer_phone.to_s.size > 5000
        raise ArgumentError.new("invalid value for \"customer_phone\", the character length must be smaller than or equal to 5000.")
      end

      @customer_phone = customer_phone
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] customer_tax_exempt Object to be assigned
    def customer_tax_exempt=(customer_tax_exempt)
      validator = EnumAttributeValidator.new("String", ["exempt", "none", "reverse"])
      unless validator.valid?(customer_tax_exempt)
        raise ArgumentError.new("invalid value for \"customer_tax_exempt\", must be one of #{validator.allowable_values}.")
      end
      @customer_tax_exempt = customer_tax_exempt
    end

    # Custom attribute writer method with validation
    # @param [Object] description Value to be assigned
    def description=(description)
      if !description.nil? && !@description.null? && description.to_s.size > 5000
        raise ArgumentError.new("invalid value for \"description\", the character length must be smaller than or equal to 5000.")
      end

      @description = description
    end

    # Custom attribute writer method with validation
    # @param [Object] footer Value to be assigned
    def footer=(footer)
      if !footer.nil? && !@footer.null? && footer.to_s.size > 5000
        raise ArgumentError.new("invalid value for \"footer\", the character length must be smaller than or equal to 5000.")
      end

      @footer = footer
    end

    # Custom attribute writer method with validation
    # @param [Object] hosted_invoice_url Value to be assigned
    def hosted_invoice_url=(hosted_invoice_url)
      if !hosted_invoice_url.nil? && !@hosted_invoice_url.null? && hosted_invoice_url.to_s.size > 5000
        raise ArgumentError.new("invalid value for \"hosted_invoice_url\", the character length must be smaller than or equal to 5000.")
      end

      @hosted_invoice_url = hosted_invoice_url
    end

    # Custom attribute writer method with validation
    # @param [Object] id Value to be assigned
    def id=(id)
      if !id.nil? && id.to_s.size > 5000
        raise ArgumentError.new("invalid value for \"id\", the character length must be smaller than or equal to 5000.")
      end

      @id = id
    end

    # Custom attribute writer method with validation
    # @param [Object] invoice_pdf Value to be assigned
    def invoice_pdf=(invoice_pdf)
      if !invoice_pdf.nil? && !@invoice_pdf.null? && invoice_pdf.to_s.size > 5000
        raise ArgumentError.new("invalid value for \"invoice_pdf\", the character length must be smaller than or equal to 5000.")
      end

      @invoice_pdf = invoice_pdf
    end

    # Custom attribute writer method with validation
    # @param [Object] number Value to be assigned
    def number=(number)
      if !number.nil? && !@number.null? && number.to_s.size > 5000
        raise ArgumentError.new("invalid value for \"number\", the character length must be smaller than or equal to 5000.")
      end

      @number = number
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] object Object to be assigned
    def object=(object)
      validator = EnumAttributeValidator.new("String", ["invoice"])
      unless validator.valid?(object)
        raise ArgumentError.new("invalid value for \"object\", must be one of #{validator.allowable_values}.")
      end
      @object = object
    end

    # Custom attribute writer method with validation
    # @param [Object] receipt_number Value to be assigned
    def receipt_number=(receipt_number)
      if !receipt_number.nil? && !@receipt_number.null? && receipt_number.to_s.size > 5000
        raise ArgumentError.new("invalid value for \"receipt_number\", the character length must be smaller than or equal to 5000.")
      end

      @receipt_number = receipt_number
    end

    # Custom attribute writer method with validation
    # @param [Object] statement_descriptor Value to be assigned
    def statement_descriptor=(statement_descriptor)
      if !statement_descriptor.nil? && !@statement_descriptor.null? && statement_descriptor.to_s.size > 5000
        raise ArgumentError.new("invalid value for \"statement_descriptor\", the character length must be smaller than or equal to 5000.")
      end

      @statement_descriptor = statement_descriptor
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] status Object to be assigned
    def status=(status)
      validator = EnumAttributeValidator.new("String", ["deleted", "draft", "open", "paid", "uncollectible", "void"])
      unless validator.valid?(status)
        raise ArgumentError.new("invalid value for \"status\", must be one of #{validator.allowable_values}.")
      end
      @status = status
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(o)
      return true if self.same?(o)
      self.class == o.class &&
        account_country == o.account_country &&
        account_name == o.account_name &&
        account_tax_ids == o.account_tax_ids &&
        amount_due == o.amount_due &&
        amount_paid == o.amount_paid &&
        amount_remaining == o.amount_remaining &&
        application_fee_amount == o.application_fee_amount &&
        attempt_count == o.attempt_count &&
        attempted == o.attempted &&
        auto_advance == o.auto_advance &&
        automatic_tax == o.automatic_tax &&
        billing_reason == o.billing_reason &&
        charge == o.charge &&
        collection_method == o.collection_method &&
        created == o.created &&
        currency == o.currency &&
        custom_fields == o.custom_fields &&
        customer == o.customer &&
        customer_address == o.customer_address &&
        customer_email == o.customer_email &&
        customer_name == o.customer_name &&
        customer_phone == o.customer_phone &&
        customer_shipping == o.customer_shipping &&
        customer_tax_exempt == o.customer_tax_exempt &&
        customer_tax_ids == o.customer_tax_ids &&
        default_payment_method == o.default_payment_method &&
        default_source == o.default_source &&
        default_tax_rates == o.default_tax_rates &&
        description == o.description &&
        discount == o.discount &&
        discounts == o.discounts &&
        due_date == o.due_date &&
        ending_balance == o.ending_balance &&
        footer == o.footer &&
        hosted_invoice_url == o.hosted_invoice_url &&
        id == o.id &&
        invoice_pdf == o.invoice_pdf &&
        last_finalization_error == o.last_finalization_error &&
        lines == o.lines &&
        livemode == o.livemode &&
        metadata == o.metadata &&
        next_payment_attempt == o.next_payment_attempt &&
        number == o.number &&
        object == o.object &&
        on_behalf_of == o.on_behalf_of &&
        paid == o.paid &&
        paid_out_of_band == o.paid_out_of_band &&
        payment_intent == o.payment_intent &&
        payment_settings == o.payment_settings &&
        period_end == o.period_end &&
        period_start == o.period_start &&
        post_payment_credit_notes_amount == o.post_payment_credit_notes_amount &&
        pre_payment_credit_notes_amount == o.pre_payment_credit_notes_amount &&
        quote == o.quote &&
        receipt_number == o.receipt_number &&
        starting_balance == o.starting_balance &&
        statement_descriptor == o.statement_descriptor &&
        status == o.status &&
        status_transitions == o.status_transitions &&
        subscription == o.subscription &&
        subscription_proration_date == o.subscription_proration_date &&
        subtotal == o.subtotal &&
        tax == o.tax &&
        threshold_reason == o.threshold_reason &&
        total == o.total &&
        total_discount_amounts == o.total_discount_amounts &&
        total_tax_amounts == o.total_tax_amounts &&
        transfer_data == o.transfer_data &&
        webhooks_delivered_at == o.webhooks_delivered_at
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(o)
      self == o
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [account_country, account_name, account_tax_ids, amount_due, amount_paid, amount_remaining, application_fee_amount, attempt_count, attempted, auto_advance, automatic_tax, billing_reason, charge, collection_method, created, currency, custom_fields, customer, customer_address, customer_email, customer_name, customer_phone, customer_shipping, customer_tax_exempt, customer_tax_ids, default_payment_method, default_source, default_tax_rates, description, discount, discounts, due_date, ending_balance, footer, hosted_invoice_url, id, invoice_pdf, last_finalization_error, lines, livemode, metadata, next_payment_attempt, number, object, on_behalf_of, paid, paid_out_of_band, payment_intent, payment_settings, period_end, period_start, post_payment_credit_notes_amount, pre_payment_credit_notes_amount, quote, receipt_number, starting_balance, statement_descriptor, status, status_transitions, subscription, subscription_proration_date, subtotal, tax, threshold_reason, total, total_discount_amounts, total_tax_amounts, transfer_data, webhooks_delivered_at].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of String => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = Stripe.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {} of String => String
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.includes?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        ({} of String => String).tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end
  end
end
